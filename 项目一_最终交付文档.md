# 项目一：Knowledge Agent — 最终交付文档 v3

> XX银行信用卡 AI 营销平台 | 负责人: A | 2026-07-17  
> 所有服务运行中: API(8080) ✅ | 360大屏(8501) ✅ | 图谱大屏(8502) ✅

---

## 一、项目定位

项目一是整个 AI 营销平台的**数据底层和知识底座**：

```
项目二(Strategy Agent) = 策略大脑 → 决定"给谁推什么"
项目三(Marketing Agent) = 对话外壳 → 执行"怎么触达怎么聊"
项目一(Knowledge Agent) = 知识引擎 → 提供"数据+画像+意图+知识"
```

---

## 二、已交付的七大模块

### 模块一: Mock 数据体系 ✅

18 张 CSV 表 + 956 ASR 对话 + 20 产品文档 + 12 活动海报，基于真实银行产品体系。

### 模块二: OneID 统一融合引擎 ✅

64,776 条 ID 映射，7 种 ID 类型 → 1 个 OneID。36/36 测试通过。

### 模块三: 客户画像与动态记忆引擎 ✅

静态画像 5 维度(人口/账户/生命周期/风险/价值) + 动态记忆 5 信号×4 窗口(实时/7d/30d/90d)。Streamlit v10 大屏可交互。

### 模块四: GraphRAG 知识图谱 ✅

187 份知识文档全文检索，15 产品→权益树形关系图。Streamlit 大屏可交互。

### 模块五: 智能意图识别 ✅

三层架构(规则引擎 + LLM 分类 + 融合)。6 类意图评分 + 5 维情感分析。19/19 测试通过。

### 模块六: 统一 API 服务 ✅

FastAPI 19 个端点，Swagger 文档在线。三个路由模块(画像/反馈/意图/知识/OneID)已统一集成。

### 模块七: 数据基座大屏 ✅

两个 Streamlit 大屏(客户 360 + 知识图谱)，支持搜索/可视化/手动输入/实时更新。

---

## 三、项目二/三如何对接（可直接使用）

### 3.1 项目二(Strategy Agent) 调用我们的接口

项目二负责人拿到这份文档后，可以直接：

```python
import requests
BASE = "http://localhost:8080"

# 步骤1: 圈选目标客群
r = requests.post(f"{BASE}/api/v1/customer/search", json={
    "age_min": 25, "age_max": 45,
    "card_level": ["白金卡", "钻石卡"],
    "value_level": ["high"],
    "exclude_dnc": True,
    "page": 1, "page_size": 100
})
customers = r.json()["customers"]
# → 拿到100个候选客户, 每人有: profile + account + consumption + risk + consent + intent

# 步骤2: 检查频控
cust_ids = [c["cust_id"] for c in customers]
r2 = requests.post(f"{BASE}/api/v1/customer/frequency-check", json={
    "cust_ids": cust_ids, "planned_channel": "短信"
})
# → 过滤掉 can_send=false 的客户

# 步骤3: 选渠道
r3 = requests.get(f"{BASE}/api/v1/channels/context")
channels = r3.json()["channels"]
# → 按成本/容量/转化率选最优渠道

# 步骤4: 查产品知识(如需要)
r4 = requests.get(f"{BASE}/api/v1/knowledge/search", params={"q": "白金卡权益", "top_k": 5})
# → 拿到产品规则/权益条款, 确保推送内容合规

# 步骤5: 做策略决策 → 把策略包发给项目三
```

### 3.2 项目三(Marketing Agent) 调用我们的接口

项目三负责人在对话智能体中：

```python
# 场景1: 客户问产品
客户: "白金卡有什么权益"
→ 智能体调用: GET /api/v1/knowledge/search?q=白金卡权益
→ 拿到答案 → LLM组织语言 → 回复客户

# 场景2: 识别客户意图
客户: "我账单太高还不上怎么办"
→ 智能体调用: POST /api/v1/intent/classify {"conversation_text": "..."}
→ 拿到: primary="分期需求", sentiment="焦虑"
→ 智能体调整话术: 温和关怀 + 推低门槛分期

# 场景3: 个性化问候
→ 智能体调用: GET /api/v1/customer/{oneid}/profile
→ 拿到客户姓名/持卡/等级 → "张先生您好! 您的白金卡享有..."
```

### 3.3 项目三回传数据给我们（C→A 反馈闭环）

项目三在每次客户交互后，回传行为数据：

```python
# 客户点击了推送
requests.post(f"{BASE}/api/v1/feedback/events", json={
    "oneid": "UID000001",
    "event_type": "click",
    "campaign_id": "CAMP_2026_DOUBLE11",
    "channel": "APP Push",
    "detail": {"stay_sec": 45}
})
# → 我们实时更新: 活跃度+1, 点击率调整, 短期窗口纳入统计

# 客户产生了消费(转化)
requests.post(f"{BASE}/api/v1/feedback/events", json={
    "oneid": "UID000001",
    "event_type": "conversion",
    "campaign_id": "CAMP_2026_DOUBLE11",
    "channel": "APP Push",
    "detail": {"amount": 5000}
})
# → 我们实时更新: 年消费+5000, ROI重算, 意图评分调整(需求被满足→降分)

# 批量回传(推荐每小时一次)
requests.post(f"{BASE}/api/v1/feedback/batch", json={
    "events": [...], "source": "project3_marketing_agent"
})
```

**我们收到回传后的处理**:

| 事件类型 | 实时处理(<100ms) | T+1批量处理(每日03:00) |
|------|------|------|
| impression | 频控计数+1 | — |
| click | 活跃度标记, 渠道偏好调整 | — |
| conversion | 年消费+amount, 意图评分重算 | 写入CSV持久化 |
| browse | 浏览偏好权重更新 | 刷新30天浏览偏好 |
| search | 意图信号重算, B端感知 | 纳入7天搜索词统计 |
| complaint | churn_risk+25, 自动降频 | 风险重评 |
| unsubscribe | consent标记更新 | 写入consent表 |

---

## 四、服务验证

| 服务 | 地址 | 验证方式 | 状态 |
|------|------|------|:--:|
| API服务 | http://localhost:8080 | `curl /api/v1/health` → 8000 customers | ✅ |
| Swagger文档 | http://localhost:8080/docs | 浏览器打开, 19个端点可在线调试 | ✅ |
| 客户360大屏 | http://localhost:8501 | 搜索 UID000001 → 5 Tab (画像/动态/意图/ROI/更新) | ✅ |
| 知识图谱大屏 | http://localhost:8502 | 选产品→权益树 / 搜索"白金卡"→5条 | ✅ |

**启动命令**:
```bash
# API (必须)
python -m uvicorn api_server.main:app --host 0.0.0.0 --port 8080

# 大屏 (可选)
streamlit run profile_engine/dashboard_customer360.py --server.port 8501
streamlit run profile_engine/dashboard_knowledge_graph.py --server.port 8502
```

---

## 五、后续改进方案

### P0: 生产化 (一期上线前必须)

| 项目 | 当前(Mock) | 目标(生产) | 工作量 |
|------|------|------|:--:|
| 图数据库 | NetworkX (内存) | Neo4j | 2天 |
| 向量化 | TF-IDF | Qwen3-Embedding-0.6B | 1天 |
| 向量存储 | 内存矩阵 | Milvus/ChromaDB | 1天 |
| 意图LLM | 关键词模拟 | Qwen3-8B API | 1天 |
| 数据存储 | CSV | PostgreSQL | 2天 |
| 实时事件 | Streamlit session | Kafka + Redis | 3天 |
| Docker化 | 无 | docker-compose 一键部署 | 1天 |

### P1: 功能增强

| 项目 | 说明 |
|------|------|
| 数据导入管理页 | Streamlit 页面: 上传CSV/PDF/图片 → 自动入库 |
| 知识图谱交互可视化 | Neo4j Browser 或 vis-network 拖拽探索 |
| LLM答案生成 | 第四步生成(Qwen3-8B)替换当前的拼接上下文 |
| 实时大屏自动刷新 | WebSocket推送画像变化 |
| AB测试分流 | 营销内容携带traffic_flag |

### P2: 体验优化

| 项目 | 说明 |
|------|------|
| 前端升级 | Streamlit → React (如需更专业UI) |
| 移动端适配 | 响应式大屏 |
| 权限管理 | API Key认证 |
| 监控告警 | Prometheus + Grafana |

---

## 六、关键文档索引

| 文档 | 用途 |
|------|------|
| [项目一_最终交付文档.md](项目一_最终交付文档.md) | 本文件 — 总交付清单 |
| [三项目接口完整规范.md](三项目接口完整规范.md) | **给项目二/三的接口手册, 可直接用** |
| [项目一_完成度总览.md](项目一_完成度总览.md) | 进度追踪(~80%) |
| [大屏使用说明_双屏详解.md](大屏使用说明_双屏详解.md) | 两个大屏操作指南 |
| [模块二 MD](模块二_OneID统一融合引擎_详细说明.md) | OneID 引擎详解 |
| [模块三 MD](模块三_客户画像与动态记忆引擎_详细说明.md) | 客户画像详解 |
| [模块四 MD](模块四_GraphRAG知识图谱_详细说明.md) | GraphRAG 详解 |
| [模块五 MD](模块五_智能意图识别引擎_详细说明.md) | 意图识别详解 |

---

> **项目一已可落地。** 项目二/三可基于[三项目接口完整规范.md](三项目接口完整规范.md)开始调用接口开发。
