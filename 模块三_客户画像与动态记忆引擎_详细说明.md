# 模块三：客户画像与动态记忆引擎 — 完整说明 v4

> Knowledge Agent | 版本 v4.0 | 代码 `profile_engine/` | 大屏 http://localhost:8501

---

## 1. card_no 碰撞 → OneID 唯一性

```
脱敏规则: 保留首4位+尾2位, 中间用*
19,939卡 → ~1,200种脱敏组合 → 平均16卡共享1个脱敏号
dict[脱敏号]=cust_id → 覆盖 → 89%客户财务数据=0

解决: id_card(强ID,不碰撞) → OneID=UID000010 是唯一标识
      + 属性驱动金融指标生成(收入×卡等级乘数×生命周期乘数)
      → 8000客户100%拥有完整画像
```

---

## 2. 静态画像 (T+1 每日03:00批量, 5维度32字段)

### 2.1 人口属性 (7字段)

| 字段 | 来源 | 示例 |
|------|------|------|
| name/gender/age | customer_basic | 张伟 / M / 32 |
| city/occupation | customer_basic | 深圳 / IT/互联网 |
| income_level | customer_basic | H(月入≥3万)/M(1-3万)/L(<1万) |
| education | customer_basic | 本科 |

### 2.2 账户属性 (8字段)

| 字段 | 公式 |
|------|------|
| `primary_card_level` | 主卡等级: 无限/钻石/白金/金卡/普卡/校园 |
| `total_credit_amount` | SUM(所有卡授信额度) |
| `used_amount` | 授信额度 × rand(10%, 85%) |
| `usage_rate` | used_amount / total_credit_amount |
| `card_count` | COUNT(该客户所有卡) |
| `active_cards` | COUNT(status=正常) |
| `tenure_months` | (now - register_date) / 30.44 |

```
卡等级消费乘数:
  无限卡=3.0 | 钻石=2.5 | 白金=1.8 | 金卡=1.2 | 普卡=0.8 | 校园=0.3
```

### 2.3 生命周期 (4字段)

```
阶段判定: months_since_open
  ≤3月   → 新户    (乘数1.2)
  3-12月 → 成长期   (乘数1.3)
  12-36月→ 成熟期   (乘数1.0)
  >36月且无近期交易 → 沉睡期 (乘数0.2)
```

### 2.4 风险标签 (8字段)

| 字段 | 公式 |
|------|------|
| `overdue_status` | 近6月逾期0次→M0, 1次→M1, 2次→M2, ≥3次→M3+ |
| `history_overdue_count_6m` | COUNT(payment_status=逾期, 近6月) |
| `min_payment_frequency_6m` | COUNT(is_min_payment=true, 近6月) |
| `cash_advance_risk_score` | 高风险(15-30)/中(5-14)/低(0-4), 按risk_level加权 |
| `churn_risk_score` | CRM系统 0-100分 |
| `risk_level` | low/medium/high, 综合逾期+投诉+流失分 |
| `blacklist_flag` | churn>85 或 DNC 标记 |
| `do_not_contact` | complaint≥3 或 dnc_list=true |

### 2.5 价值标签 (7字段)

| 字段 | 公式 |
|------|------|
| `annual_consumption` | 月收入 × 消费收入比(20%-70%) × 12 × 卡等级乘数 × 生命周期乘数 × rand(0.6,1.4) |
| `monthly_avg_consumption` | annual_consumption / 12 |
| `max_single_transaction` | annual × rand(3%, 25%) |
| `transaction_count_12m` | annual / rand(200, 1500) |
| `installment_contribution_12m` | annual × rand(3%, 12%) |
| `value_level` | high/medium/low, 综合年消费+收入等级 |

---

## 3. 动态记忆 (5信号 × 4时间窗口)

### 3.1 信号定义

| 信号 | 含义 | 短期(7d) | 中期(30d) | 长期(90d) | 实时(≤1h) |
|------|------|:--:|:--:|:--:|:--:|
| 消费趋势 | 消费金额+方向 | 总额 | 趋势↑↓→ | 活跃度评分 | 大额/境外标记 |
| 搜索意图 | 高频搜索词 | Top5关键词 | — | — | — |
| 浏览偏好 | APP浏览分布 | — | Top5页面+占比 | — | — |
| 关键事件 | 里程碑事件 | — | — | 里程碑列表 | 实时信号数 |
| 活跃度 | 综合活跃评分 | 天数 | 天数 | 0-100评分 | — |

### 3.2 四窗口示意图

```
Day-90              Day-30            Day-7          Day-1       Now
  │                   │                 │              │          │
  │←── 长期90d(T+1) ──→│                 │              │          │
  │  消费¥45,000       │                 │              │          │
  │  活跃度62/100       │                 │              │          │
  │  信号:消费降级       │                 │              │          │
  │                   │←── 中期30d(T+1)─→│              │          │
  │                   │  消费¥12,000     │              │          │
  │                   │  趋势↓20%(降级)   │              │          │
  │                   │  浏览:权益60%     │              │          │
  │                   │                 │← 短期7d(1h)─→│          │
  │                   │                 │  消费¥3,200    │          │
  │                   │                 │  搜索:出境游×8  │          │
  │                   │                 │              │←实时→│
  │                   │                 │              │ 搜索分期 │
  │                   │                 │              │ 浏览计算器│
```

### 3.3 活跃度评分公式 (0-100分)

```
天分 = min( active_days_90d / 90 × 40, 40 )   ← 占40%
笔分 = min( txn_count_90d / 100 × 30, 30 )    ← 占30%
额分 = min( consumption_90d / 50000 × 30, 30 ) ← 占30%
总分 = 天分 + 笔分 + 额分

评级: ≥60→高活跃 | 30-59→中活跃 | <30→低活跃

为什么很多客户是low(低)?
  - 校园卡客户: 乘数0.3 → 年消费仅~¥21K → 90天消费~¥5K → 额分=3分 → 总分<30
  - 沉睡期客户: 乘数0.2 → 几乎无交易 → 总分≈0
  - 普卡客户: 乘数0.8 → 消费中等 → 总分30-50
  - 金卡活跃: 乘数1.2 + 收入中等 → 总分40-60
  - 白金/钻石: 乘数1.8-2.5 + 高收入 → 总分60-85
  - 无限卡: 乘数3.0 + 高收入 → 总分70-95
```

### 3.4 沉睡风险判定

```
dormancy_risk = 
  activity_score < 20  → high   (沉睡预警, 需要唤醒策略)
  activity_score 20-49 → medium (需关注, 活跃度偏低)
  activity_score ≥ 50  → low    (正常活跃)
```

---

## 4. "实时更新" 的含义

### 4.1 三层更新机制

```
┌──────────────────────────────────────────────────────────┐
│ 实时窗口 (Session内存, <100ms)                            │
│  ├─ 手动输入: 点击"发送事件" → 增量写入 st.session_state   │
│  ├─ API批量: POST /api/v1/feedback/batch → 同上          │
│  ├─ 显示: 画像中显示为 "实时 +¥XXX" 的delta值              │
│  └─ 注意: 不写CSV! 仅当前Session可见, 刷新页面后清空       │
├──────────────────────────────────────────────────────────┤
│ 微批处理 (每小时)                                         │
│  ├─ 短期7d窗口聚合                                         │
│  ├─ 降级/活跃度骤降信号检测                                 │
│  └─ 当前mock阶段用Pandas模拟                               │
├──────────────────────────────────────────────────────────┤
│ T+1批处理 (每日03:00)                                     │
│  ├─ 全量刷新静态画像                                       │
│  ├─ 中长期窗口聚合                                         │
│  ├─ 汇总今天所有实时增量 → 写入CSV持久化                     │
│  └─ 这才是真正"存入数据库"的时刻                             │
└──────────────────────────────────────────────────────────┘
```

### 4.2 为什么手动输入后年消费没有永久变化

```
你输入 ¥5000 转化事件:
  → 实时窗口: 年消费+5000 (显示在st.session_state中)
  → 屏幕显示: "实时 +¥5,000"
  → 但不写入CSV!
  
下次T+1批量(每日03:00):
  → 汇总今天所有实时增量
  → 年消费 = CSV中的原始值 + 今天累积的所有delta
  → 写入CSV, 持久化
  → 第二天刷新页面后, 年消费永久更新了
```

### 4.3 生产环境 vs Mock环境

| | Mock环境 (当前) | 生产环境 |
|------|------|------|
| 实时窗口 | `st.session_state` (内存) | Redis ZSET |
| 微批处理 | Pandas (手动触发) | APScheduler + Redis |
| T+1批量 | Pandas (手动重跑脚本) | APScheduler + PostgreSQL |
| 事件源 | 手动输入/API模拟 | Kafka消费者 |

---

## 5. C→A 反馈接口 (项目三回传)

### 8种事件类型及画像更新

| event_type | 实时更新 | T+1汇总 |
|-----------|------|------|
| `conversion` | 年消费+amount, 月均+amount/12, 笔数+1 | 写入value_annual_consumption等CSV字段 |
| `click` | 活跃度+1, 渠道偏好权重更新 | 纳入点击率统计 |
| `browse` | 浏览偏好权重更新 | 纳入mid_term_30d_browse_preferences |
| `search` | 意图评分重算 | 纳入short_term_7d_top_search_keywords |
| `conversation` | LLM重算意图向量+情感标签 | 纳入事件流 |
| `impression` | 频控计数+1 | 纳入频控统计 |
| `complaint` | churn_risk+25, 自动降频30天 | 纳入投诉统计+风险重评 |
| `unsubscribe` | channel_consent→false | 永久写入consent表 |

### API端点

```http
POST /api/v1/feedback/events        # 单条
POST /api/v1/feedback/batch         # 批量
POST /api/v1/feedback/transaction   # 手动交易
```

---

## 6. Streamlit 大屏

**http://localhost:8501**

| Tab | 内容 |
|-----|------|
| 静态画像 | 5维度32字段 + 实时增量标注 + 更新机制说明 |
| 动态记忆 | 5信号卡片(含公式) + 四窗口示意图 + 趋势图 + 雷达图 |
| 活动效果 | 参与/转化/收入/ROI + 归因明细 |
| 实时更新 | 手动输入8种事件 + 批量API + 更新日志 + **活跃度公式详解** |

**每个字段下都有 `.formula` 标注的计算公式**

---

> **代码**: `profile_engine/` | **数据**: `mock_data/structured/customer_profile.csv` (8000×58)  
> **启动**: `streamlit run profile_engine/dashboard_customer360.py`
