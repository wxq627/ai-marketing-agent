# 模块五：智能意图识别引擎 — 详细说明 v2

> Knowledge Agent | 代码 `intent_engine/` | 测试 19/19 PASS

---

## 五分钟看懂（先读这里）

### Q1: 当前用的是 LLM 还是 Qwen3-8B？

**现在用的是关键词规则模拟 LLM**。代码里有一个开关：

```python
class LLMClassifier:
    def __init__(self, mock_mode: bool = True):  # ← 现在是 True
        self.mock_mode = mock_mode
```

| 模式 | `mock_mode` | 实际行为 | 什么时候用 |
|------|:--:|------|------|
| **Mock 模式（当前）** | `True` | 用关键词匹配模拟 LLM 输出，0 成本，<5ms | 开发、测试、演示 |
| **生产模式（未来）** | `False` | 调用 Qwen3-8B API，真正理解语义 | 上线后 |

> Qwen3-8B **是** LLM 的一种。就像"汽车"和"特斯拉 Model 3"的关系——LLM 是类别，Qwen3-8B 是具体型号。

### Q2: 两层是加权求和吗？

**不是简单加权。是各干各的，最后合并。**

```
第一层（规则引擎）              第二层（LLM分类器）
     │                               │
     │ 输入: 结构化数据               │ 输入: 对话文本
     │ (搜索词/浏览页面/账单金额)      │ (ASR通话记录)
     │                               │
     │ 方法: 每条规则检查是否触发      │ 方法: 关键词匹配 / Qwen3-8B
     │ 触发 → 加对应的权重分          │ 理解语义 → 给出意图+情感
     │                               │
     │ 输出: 6类意图评分(0-100)       │ 输出: 主要意图 + 情感 + 关键短语
     │ + 子信号明细                   │
     │                               │
     └──────────┬────────────────────┘
                ▼
        融合输出（第三层）
     {
       "intents": 6类全部评分,    ← 来自规则引擎
       "sub_signals": 可解释,    ← 来自规则引擎
       "sentiment": 情感,        ← 来自LLM
       "primary_intent": 主意图  ← 综合判定
     }
```

**两层不是"各打 50 分加起来"**。规则引擎独立打出 6 类分数（0-100），LLM 也独立分类。融合时各取所长：规则出分+子信号，LLM 出情感+语义。

### Q3: 六类意图是什么？

```
┌─────────────────────────────────────────────────────────────┐
│  ① 分期/借贷需求    客户想借钱、想分期还款                     │
│  ② 跨境/出行需求    客户要出国、要旅游                         │
│  ③ 额度/升级需求    客户觉得额度不够、想升级更好的卡              │
│  ④ 权益/优惠需求    客户想用积分、想薅羊毛                       │
│  ⑤ 沉睡/流失风险    客户很久没用了、可能要销户                   │
│  ⑥ 新户/激活引导    刚开卡不知道怎么用、还没刷第一笔              │
└─────────────────────────────────────────────────────────────┘
```

### Q4: 置信度用来干什么？

**告诉 B 端"这个分数有多可靠"，决定要不要立即行动。**

| 置信度 | 分数范围 | 含义 | B 端该怎么做 |
|:--:|:--:|------|------|
| **high** | ≥70 | 信号强烈，多重交叉验证 | **立即触达**，这是最佳时机 |
| **medium** | 40-69 | 有一定信号，但不够确定 | 持续观察，可以触达但不要太激进 |
| **low** | <40 | 信号微弱或没有 | 别打扰客户 |

> 举例：分期需求 85 分(high) → B 端立即推送分期方案。分期需求 45 分(medium) → B 端标记"可能有意向, 下周再观察"。分期需求 15 分(low) → 不推送分期内容。

### Q5: 具体规则是什么？

每条意图由**多条触发规则**组成，每条规则有：**信号来源 + 触发条件 + 权重分数**。

以"分期/借贷需求"为例：

```
分期需求总分 = 0（起始）
  + 30分  如果 账单金额/授信额度 > 70%        ← 账单快还不上了
  + 20分  如果 近7天搜索"分期"≥2次            ← 主动在找分期方案
  + 25分  如果 客服通话中提到"账单压力"        ← 已经急了，打电话求助
  + 10分  如果 近3月用过最低还款               ← 确实手头紧
  + 15分  如果 近7天浏览分期计算器≥3次         ← 在算每月要还多少
  ─────
  最高 100 分
```

每个信号都**可追溯**——你知道这 85 分是怎么来的：账单压力 30 + 搜索 20 + 客服求助 25 + 最低还款 10。

### Q6: 输出是六种意图 + 情感分析吗？

**是的。每个客户都输出：6 类意图评分 + 1 个主意图 + 情感分析。**

```json
{
  "oneid": "UID000001",
  "primary_intent": "分期/借贷需求",     ← 1 个主意图（6个里分数最高的）
  "intents": [                           ← 6 类意图全部输出
    {"type": "分期/借贷需求", "score": 85, "confidence": "high", "sub_signals": [...]},
    {"type": "跨境/出行需求", "score": 0,  "confidence": "low"},
    {"type": "额度/升级需求", "score": 0,  "confidence": "low"},
    {"type": "权益/优惠需求", "score": 0,  "confidence": "low"},
    {"type": "沉睡/流失风险", "score": 0,  "confidence": "low"},
    {"type": "新户/激活引导", "score": 0,  "confidence": "low"}
  ],
  "sentiment": {                          ← 情感分析（独立于意图）
    "overall": "焦虑",
    "anxiety_score": 75,
    "satisfaction_score": 30
  }
}
```

> 注意：一个客户可能同时有多个意图！比如：分期 85 分 + 出行 45 分 + 权益 30 分。B 端可以同时推"分期方案"和"出境游返现卡"。

---

## 完整详解

### 1. 功能定位

```
你的职责（A 端）                          B 端的职责
────────────────                        ────────────
识别"客户现在想要什么"          ─→       根据意图决定"推什么策略"
输出意图向量（IntentVector）    ─→       读取意图向量做策略决策

你只需要告诉 B 端：
  "这个客户当前有 85% 的概率想要分期，
   同时有 40% 的概率对出境游感兴趣，
   整体情绪偏焦虑。"
```

### 2. 为什么用三层架构

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|:--:|
| 只用规则 | 快、可解释、0成本 | 不懂"我快撑不住了"这种隐含语义 | ❌ |
| 只用 LLM | 理解力强 | 慢、贵、不可控、可能胡说 | ❌ |
| **规则 + LLM** | **快慢互补、可解释+可理解** | 架构稍复杂 | ✅ |

**更深层的理由**：80% 的意图信号来自结构化数据（搜索词、浏览页面、账单金额），规则直接够用，不需要 LLM。20% 来自对话文本，才需要 LLM。把 LLM 放第二层低频调用，不放第一层全量跑。

### 3. 第一层：规则评分引擎 (`rule_scorer.py`)

#### 3.1 方法：加权规则评分

```
对每条意图：
  总分 = 0
  遍历该意图的所有触发规则：
    如果满足触发条件 → 总分 += 该规则的权重
  限制总分 ≤ 100
```

#### 3.2 为什么不用机器学习

信用卡意图的信号**极其明确**：搜索"分期"=想分期，搜索"销户"=想销户。不需要 ML 来"发现"这个规律。

| 维度 | 规则引擎 | ML模型 |
|------|:--:|:--:|
| 可解释性 | ✅ "账单/额度比78%→+30分" | ❌ 黑盒 |
| 修改成本 | ✅ 改 YAML 即可 | ❌ 重训练+部署 |
| 实时性 | ✅ <1ms | ~10-100ms |
| 冷启动 | ✅ 专家直接定义 | ❌ 需大量标注 |

#### 3.3 每条规则的 sub_signals（可解释性核心）

这是规则引擎最重要的输出。B 端不只是拿到一个分数，还知道**这个分数是怎么来的**：

```json
"sub_signals": [
  {
    "signal": "账单金额/授信额度比",  ← 哪个信号
    "value": "78%",                   ← 实际值是多少
    "weight": 30,                     ← 贡献了多少分
    "threshold": "gt 0.7"             ← 触发阈值是什么
  }
]
```

> 答辩时可以直接说："分期意图 85 分的 85 分是这样构成的：账单压力 30 分 + 搜索信号 20 分 + 客服求助 25 分 + 最低还款 10 分。"

#### 3.4 6 类意图完整规则表

**① 分期/借贷需求**

| 触发信号 | 数据来源 | 条件 | 贡献分数 | 为什么这个权重 |
|------|------|:--:|:--:|------|
| 账单金额 / 授信额度 > 70% | bill_record + credit_card | >0.70 | **30** | 最直接的还款压力指标 |
| 近 7 天搜索"分期""手续费"等 ≥2 次 | app_events 搜索词 | ≥2 | **20** | 主动搜索 = 强意图 |
| 客服通话中提到分期/账单压力 ≥1 次 | ASR 对话 | ≥1 | **25** | 人工求助 = 急迫需求 |
| 近 3 月最低还款 ≥1 次 | bill_record | ≥1 | **10** | 还款能力偏弱 |
| 近 7 天浏览"分期计算器" ≥3 次 | app_events 浏览 | ≥3 | **15** | 正在计算 = 正在考虑 |

**② 跨境/出行需求**

| 触发信号 | 条件 | 贡献分数 |
|------|:--:|:--:|
| 近 30 天境外交易 ≥3 笔 | ≥3 | 25 |
| 近 7 天搜索"境外""汇率""海淘" ≥2 次 | ≥2 | 20 |
| 近 7 天搜索旅游目的地（东京/巴黎等）≥3 次 | ≥3 | 20 |
| 持有境外返现权益但近 3 月未使用 | 存在 | 15 |

**③ 额度/升级需求**

| 触发信号 | 条件 | 贡献分数 |
|------|:--:|:--:|
| 近 90 天额度使用率 > 85% | >0.85 | 25 |
| 近 7 天搜索"提额""额度不够" ≥1 次 | ≥1 | 25 |
| 收入等级 H 但卡等 ≤ 金卡 | 满足 | 15 |
| 近 30 天浏览高端卡产品页 ≥3 次 | ≥3 | 20 |

**④ 权益/优惠需求**

| 触发信号 | 条件 | 贡献分数 |
|------|:--:|:--:|
| 近 7 天浏览"权益商城" ≥5 次 | ≥5 | 25 |
| 近 7 天搜索"积分""兑换""优惠" ≥2 次 | ≥2 | 20 |
| 近 30 天浏览合作商户页面 ≥3 次 | ≥3 | 15 |
| 搜索"星巴克""电影票""贵宾厅" ≥1 次 | ≥1 | 15 |

**⑤ 沉睡/流失风险** ⚠️ 特别注意

| 触发信号 | 条件 | 贡献分数 |
|------|:--:|:--:|
| 连续无主动交易 > 90 天 | >90 | 30 |
| 额度使用率持续下降 > 30% | >0.30 | 20 |
| 近 30 天有客服投诉 ≥1 次 | ≥1 | 25 |
| 近 7 天搜索"销户""注销" ≥1 次 | ≥1 | 25 |

> ⚠️ score ≥ 50 即为高风险——流失不可逆，B 端需**优先处理**。

**⑥ 新户/激活引导**

| 触发信号 | 条件 | 贡献分数 |
|------|:--:|:--:|
| 生命周期 = 新户（开卡 0-3 月） | 满足 | 25 |
| 卡状态正常但开卡 >7 天无交易 | 满足 | 25 |
| 绑定快捷支付但未产生交易 | 满足 | 15 |
| 浏览"新手引导" ≥1 次 | ≥1 | 10 |

#### 3.5 置信度判定

```
score ≥ 70  → confidence = "high"   → 强烈信号，立即触达
score 40-69 → confidence = "medium" → 有一定信号，持续观察
score < 40  → confidence = "low"    → 信号微弱，不打扰

例外: 沉睡/流失风险的 high 阈值降为 50（流失不可逆，宁可多报）
```

#### 3.6 趋势判断

```
当前分数 vs 7天前分数:
  上升 >15%  → trend = "rising"   → 需求升温，好时机
  变化 ±15%  → trend = "stable"   → 需求稳定
  下降 >15%  → trend = "falling"  → 需求消退，或已被满足
```

---

### 4. 第二层：LLM 语义分类 (`llm_classifier.py`)

#### 4.1 当前实现：关键词匹配模拟 LLM

```python
# 这是当前 mock_mode=True 的实际代码逻辑
intent_keywords = {
    "分期/借贷需求": ["分期", "手续费", "账单", "还不上", "压力", "12期"],
    "跨境/出行需求": ["境外", "出国", "汇率", "海淘", "签证", "机票", "东京"],
    ...
}

def classify(text):
    for intent, keywords in intent_keywords.items():
        count = 匹配到的关键词数量
        score = count × 15 + random(-5, +20)  # 模拟LLM评分波动
    return 最高分的意图
```

#### 4.2 生产环境：替换为真实 LLM API

```python
# 当 mock_mode=False 时：
def classify(text):
    prompt = load_prompt("intent_classify.txt")  # 加载提示词模板
    response = call_qwen_api(prompt + text)       # 调用 Qwen3-8B
    return parse_json(response)                   # 解析 JSON 输出
```

#### 4.3 LLM 输出的内容

```json
{
  "primary_intent": "分期需求",     // 主意图
  "intent_score": 85,               // 置信评分
  "sentiment": "焦虑",              // 情感
  "urgency": "高",                  // 紧急程度
  "key_phrases": ["账单太高", ...]  // 关键短语（可解释性）
}
```

---

### 5. 情感分析 (`SentimentAnalyzer`)

独立于意图分类，输出 5 种情感 + 对 B 端的策略建议。

| 情感 | 焦虑度 | 满意度 | 触发信号 | B 端策略 |
|------|:--:|:--:|------|------|
| **满意** | 10 | 80 | 客服评价高、持续活跃 | 可推送升级/高端权益，话术积极 |
| **中性** | 30 | 60 | 日常正常使用 | 按标准策略执行 |
| **焦虑** | 75 | 30 | "还不上""压力大" | 优先低门槛分期，话术温和关怀 |
| **不满** | 60 | 15 | 投诉、搜索"投诉" | 避免营销，先安抚 |
| **好奇** | 20 | 55 | 浏览新产品、搜索对比 | 推送详细介绍，引导转化 |

> **情感+意图的组合使用**：同样是"分期需求 85 分"，焦虑客户→温和话术+低门槛方案，中性客户→标准推送即可。

---

### 6. 完整数据流（一次查询的全过程）

```
B 端调用: GET /api/v1/customer/UID000001/intent
                │
                ▼
    加载该客户的画像数据 (customer_profile.csv)
    + 搜索关键词 (search_keywords_7d)
    + 浏览偏好 (browse_preferences_30d)
    + 生命周期/风险/账单数据
                │
        ┌───────┴───────┐
        ▼               ▼
   第一层: 规则引擎    第二层: LLM分类器
   (总是运行)         (仅当有对话文本时)
        │               │
        │ 6类意图       │ 主意图+情感
        │ sub_signals   │ key_phrases
        │ confidence    │ urgency
        │ trend         │
        └───────┬───────┘
                ▼
         第三层: 融合
         {
           primary_intent: "分期/借贷需求",  ← 规则+LLM综合
           intents: [6类评分+子信号],        ← 规则引擎
           sentiment: {情感},               ← LLM
           trend: "rising"                  ← 规则引擎
         }
                │
                ▼
          返回给 B 端
```

---

### 7. API 接口

| # | 方法 | 路径 | 输入 | 输出 |
|:--:|:--:|------|------|------|
| 1 | GET | `/api/v1/customer/{oneid}/intent` | OneID | 6意图+情感 |
| 2 | POST | `/api/v1/intent/classify` | 对话文本 | 意图+情感+关键短语 |
| 3 | POST | `/api/v1/intent/batch` | OneID列表 | 批量意图 |

---

### 8. 性能指标

| 指标 | 规则引擎 | LLM(Mock) | LLM(生产Qwen3-8B) |
|------|:--:|:--:|:--:|
| 每次延迟 | <1ms | <5ms | 500ms-2s |
| 每次成本 | ¥0 | ¥0 | ~¥0.001 |
| 每秒吞吐 | 10,000+ | 10,000+ | ~10 |
| 可解释性 | ✅ sub_signals | ⚠️ key_phrases | ⚠️ |

---

### 9. 输出示例（完整版）

```json
{
  "oneid": "UID000001",
  "updated_at": "2026-07-17T14:30:00Z",
  "primary_intent": "分期/借贷需求",
  "intents": [
    {
      "type": "分期/借贷需求",
      "score": 85,
      "confidence": "high",
      "trend": "rising",
      "sub_signals": [
        {"signal": "账单金额/授信额度比", "value": "78%", "weight": 30, "threshold": "gt 0.7"},
        {"signal": "近7天搜索分期关键词", "value": "匹配4个关键词", "weight": 20, "threshold": "gte 2"},
        {"signal": "近7天客服通话涉及分期", "value": "匹配1个关键词", "weight": 25, "threshold": "gte 1"},
        {"signal": "近3月最低还款次数", "value": "2次", "weight": 10, "threshold": "gte 1"}
      ],
      "evidence_summary": "账单78%→30分; 搜索分期×4→20分; 客服求助→25分; 最低还款2次→10分"
    },
    {"type": "跨境/出行需求", "score": 45, "confidence": "medium", "trend": "rising",
     "sub_signals": [
       {"signal": "近7天搜索境外/出行关键词", "value": "匹配3个关键词", "weight": 20},
       {"signal": "持有境外返现权益但未使用", "value": "存在", "weight": 15}
     ]},
    {"type": "额度/升级需求", "score": 0, "confidence": "low"},
    {"type": "权益/优惠需求", "score": 30, "confidence": "low"},
    {"type": "沉睡/流失风险", "score": 0, "confidence": "low"},
    {"type": "新户/激活引导", "score": 0, "confidence": "low"}
  ],
  "sentiment": {
    "overall": "焦虑",
    "confidence": 0.85,
    "anxiety_score": 75,
    "satisfaction_score": 30,
    "key_evidence": "客服通话中多次表达'账单太高''压力大'等焦虑信号",
    "b_strategy_impact": "优先推荐低门槛分期, 话术需温和关怀"
  }
}
```

**B 端拿到这份数据后的决策**：
1. 主意图=分期(85分,high,rising) → **立即触达**
2. 次要意图=出行(45分,medium,rising) → 可以顺带推境外返现卡
3. 情感=焦虑(焦虑75/满意30) → **话术要温和**，强调"低门槛""不着急"
4. 分期 85 分的构成可解释：账单压力(30)+搜索(20)+客服求助(25) → **可信**

---

### 10. 测试结果 19/19

```
[1] 规则引擎 6/6: 分期85(high) 沉睡50(high) 新户35(medium) 出行0(low)
[2] LLM 5/5: 分期对话→分期72焦虑  出行对话→出行10中性
[3] 情感 5/5: 焦虑75/30 满意10/80 中性30/60 不满60/15 好奇20/55
[4] 趋势 3/3: rising✅ falling✅ stable✅
```

> **代码**: `intent_engine/` | **规则配置**: `intent_rules.yaml` | **启动**: 集成于 `api_server/main.py`
