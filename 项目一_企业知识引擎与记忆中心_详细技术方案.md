# 项目一：企业知识引擎与记忆中心（Knowledge Agent）详细技术方案

> **项目背景**：AI 智能营销 Agent —— 信用卡全链路营销智能平台（新一代 AI 营销中枢）
>
> **负责人**：A（偏大数据、RAG 底层）
>
> **角色定位**：建设整个系统的“底层认知基石”，统一企业级知识与记忆底座；解决“认清客户”和“多源知识融合”
>
> **文档版本**：v1.0
>
> **最后更新**：2026-07-15

---

## 目录

- [一、角色定位与总体架构](#一角色定位与总体架构)
- [二、模块一：Mock 数据体系设计与生成](#二模块一mock-数据体系设计与生成)
- [三、模块二：OneID 统一融合引擎](#三模块二oneid-统一融合引擎)
- [四、模块三：客户画像与动态记忆引擎](#四模块三客户画像与动态记忆引擎)
- [五、模块四：GraphRAG 企业知识图谱构建](#五模块四graphrag-企业知识图谱构建)
- [六、模块五：智能意图识别与非结构化洞察](#六模块五智能意图识别与非结构化洞察)
- [七、模块六：统一 API 服务层](#七模块六统一-api-服务层)
- [八、模块七：数据基座大屏与数据管理页面](#八模块七数据基座大屏与数据管理页面)
- [九、技术栈与开源模型选型](#九技术栈与开源模型选型)
- [十、协同集成流与标准数据接口](#十协同集成流与标准数据接口)
- [十一、最终交付成果清单](#十一最终交付成果清单)
- [十二、执行路线图与里程碑](#十二执行路线图与里程碑)
- [附录 A：JSON 接口契约规范](#附录-ajson-接口契约规范)
- [附录 B：意图分类体系详细定义](#附录-b意图分类体系详细定义)

---

## 一、角色定位与总体架构

### 1.1 项目总体定位

本项目基于“明确经营策略 → 认清客户 → 策略触达客户”的稳定三角运营理念，构建一套面向信用卡业务的端到端 AI 营销平台，形成“数据理解 → 策略制定 → 客户触达 → 效果反馈 → 持续优化”的能力闭环。

三位实习生的分工完全契合核心业务架构中的“知识/数据”、“B 端策略”、“C 端触达”三足鼎立关系：

| 子项目 | 负责人 | 核心职责 | 技术对标 |
|--------|--------|---------|---------|
| **项目一：企业知识引擎与记忆中心** | **A** | 统一企业级知识与记忆底座；解决“认清客户”和“多源知识融合” | GraphRAG、动态画像、实时意图感知 |
| 项目二：策略编排与推荐大脑 | B | 搭建系统“经营大脑”；解决“如何经营客户” | 可解释策略、动态追问、MCP 工具链 |
| 项目三：C 端智能交互与触达执行 | C | 扮演系统的“手脚与感官”；解决“如何精准触达与转化” | 多模态 AIGC、多渠道路由、闭环回流 |

### 1.2 项目一整体架构

```
┌────────────────────────────────────────────────────────────────────┐
│                    项目一：企业知识引擎与记忆中心                       │
│                     Knowledge Agent 系统架构                         │
├────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │ 银行核心   │  │   CRM    │  │ APP 埋点  │  │ 客服 ASR  │           │
│  │ (交易/账单)│  │ (客户关系)│  │ (行为事件)│  │ (对话文本)│           │
│  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘           │
│        └──────────────┴──────────────┴──────────────┘                │
│                              │                                       │
│                     ┌────────▼────────┐                              │
│                     │   OneID 融合引擎  │  ← 模块二                    │
│                     │  统一客户主体标识  │                              │
│                     └────────┬────────┘                              │
│                              │                                       │
│              ┌───────────────┼───────────────┐                       │
│              │               │               │                       │
│     ┌────────▼────────┐ ┌───▼──────┐ ┌──────▼────────┐              │
│     │  客户画像引擎     │ │ GraphRAG │ │  意图识别引擎  │              │
│     │  (模块三)        │ │ (模块四)  │ │  (模块五)      │              │
│     │ ·静态画像        │ │ ·知识图谱│ │ ·规则评分      │              │
│     │ ·动态记忆        │ │ ·向量检索│ │ ·大模型语义    │              │
│     └────────┬────────┘ └────┬─────┘ └──────┬────────┘              │
│              │               │               │                       │
│              └───────────────┼───────────────┘                       │
│                              │                                       │
│                     ┌────────▼────────┐                              │
│                     │  统一 API 服务层  │  ← 模块六                    │
│                     │  (FastAPI REST)  │                              │
│                     └────────┬────────┘                              │
│                              │                                       │
│              ┌───────────────┼───────────────┐                       │
│              │               │               │                       │
│     ┌────────▼────────┐ ┌───▼──────┐ ┌──────▼────────┐              │
│     │  数据基座大屏     │ │ B端策略  │ │ C端触达执行    │              │
│     │  (模块七)        │ │ 大脑     │ │ Agent          │              │
│     └─────────────────┘ └──────────┘ └───────────────┘              │
│                                                                     │
└────────────────────────────────────────────────────────────────────┘
```

### 1.3 数据流向

```
多源异构数据 → OneID 融合 → 画像/图谱/意图 → 统一 API → 供 B/C 端消费
                                    ↑                              │
                                    │       反馈回流               │
                                    └──────────────────────────────┘
```

---

## 二、模块一：Mock 数据体系设计与生成

### 2.1 设计目标

构建一套覆盖银行信用卡全业务场景的仿真数据集，涵盖结构化表格、半结构化对话文本、非结构化文档及图像等多种形态，为后续所有模块提供数据基础。

### 2.2 数据总体规模

| 指标 | 数值 |
|------|------|
| 客户总数 | 5,000 ~ 10,000 |
| 信用卡产品 | 8 ~ 12 |
| 权益项 | 50 ~ 100 |
| 营销活动 | 20 ~ 30 |
| 交易流水 | 约 50 万条（12 个月） |
| APP 埋点事件 | 约 50 万条 |
| 客服 ASR 对话 | 500 ~ 2,000 条 |
| 非结构化文档 | 20 ~ 50 份（PDF/图片） |

### 2.3 结构化数据（表格/数据库形式）

#### 2.3.1 银行核心系统数据

**表 1：`customer_basic`（客户基础信息表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `cust_id` | VARCHAR(32) | 银行核心客户 ID | `C000001` |
| `name` | VARCHAR(64) | 脱敏姓名 | `张*明` |
| `gender` | CHAR(1) | 性别 | `M` / `F` |
| `age` | INT | 年龄 | `32` |
| `city` | VARCHAR(32) | 所在城市 | `深圳` |
| `occupation` | VARCHAR(32) | 职业类别 | `IT/互联网` |
| `income_level` | VARCHAR(8) | 收入等级 | `H`(高) / `M`(中) / `L`(低) |
| `education` | VARCHAR(16) | 学历 | `本科` |
| `id_card` | VARCHAR(32) | 脱敏身份证号 | `4403****001` |
| `phone` | VARCHAR(16) | 脱敏手机号 | `138****8888` |
| `register_date` | DATE | 开户日期 | `2020-03-15` |

**表 2：`credit_card`（信用卡表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `card_no` | VARCHAR(32) | 脱敏卡号 | `6225****01` |
| `cust_id` | VARCHAR(32) | 客户 ID（外键） | `C000001` |
| `card_level` | VARCHAR(16) | 卡等级 | `普卡` / `金卡` / `白金卡` / `无限卡` / `钻石卡` |
| `credit_amount` | DECIMAL(12,2) | 授信额度 | `200000.00` |
| `open_date` | DATE | 开卡日期 | `2020-03-20` |
| `card_status` | VARCHAR(16) | 卡片状态 | `正常` / `冻结` / `销卡` |
| `product_id` | VARCHAR(32) | 产品 ID | `PROD_WHITE001` |

> **注意**：一个客户可持有同一银行的多张信用卡，通过 `cust_id` 关联，OneID 引擎会统一处理。

**表 3：`transaction_log`（交易流水表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `txn_id` | VARCHAR(32) | 交易唯一 ID | `T000001` |
| `card_no` | VARCHAR(32) | 卡号（外键） | `6225****01` |
| `txn_type` | VARCHAR(16) | 交易类型 | `消费` / `取现` / `还款` / `退款` / `分期` |
| `amount` | DECIMAL(12,2) | 交易金额 | `158.00` |
| `currency` | VARCHAR(8) | 币种 | `CNY` / `USD` / `EUR` |
| `merchant_category` | VARCHAR(32) | 商户类别 | `餐饮` / `商旅` / `购物` / `境外` / `娱乐` / `教育` |
| `merchant_name` | VARCHAR(64) | 商户名称 | `海底捞` |
| `is_cross_border` | BOOLEAN | 是否境外交易 | `true` / `false` |
| `txn_channel` | VARCHAR(16) | 交易渠道 | `线下刷卡` / `支付宝` / `微信` / `银联在线` / `Apple Pay` |
| `timestamp` | DATETIME | 交易时间 | `2026-07-01 12:30:00` |

**表 4：`bill_record`（账单记录表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `bill_id` | VARCHAR(32) | 账单 ID | `B000001` |
| `card_no` | VARCHAR(32) | 卡号 | `6225****01` |
| `bill_month` | VARCHAR(7) | 账单月份 | `2026-07` |
| `bill_amount` | DECIMAL(12,2) | 本期账单金额 | `18500.00` |
| `min_payment` | DECIMAL(12,2) | 最低还款额 | `1850.00` |
| `is_min_payment` | BOOLEAN | 是否最低还款 | `true` / `false` |
| `due_date` | DATE | 到期还款日 | `2026-08-05` |
| `payment_status` | VARCHAR(16) | 还款状态 | `已还清` / `最低还款` / `逾期` |

#### 2.3.2 CRM 系统数据

**表 5：`crm_customer`（客户关系管理表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `crm_id` | VARCHAR(32) | CRM 系统 ID | `CRM000001` |
| `cust_id` | VARCHAR(32) | 关联银行核心客户 ID | `C000001` |
| `lifecycle_stage` | VARCHAR(16) | 生命周期阶段 | `新户` / `成长期` / `成熟期` / `沉睡期` |
| `customer_manager` | VARCHAR(32) | 客户经理 | `王XX` |
| `vip_tier` | VARCHAR(16) | VIP 等级 | `钻石` / `白金` / `金卡` / `普通` |
| `churn_risk_score` | INT | 流失风险评分（0-100） | `25` |
| `last_contact_date` | DATE | 最近联系日期 | `2026-06-15` |
| `contact_preference` | VARCHAR(16) | 联系偏好 | `短信` / `APP Push` / `电话` / `邮件` |

> **生命周期阶段说明**：
> - **新户**：开卡 0-3 个月
> - **成长期**：开卡 3-12 个月，活跃度上升
> - **成熟期**：开卡 12-36 个月，消费稳定
> - **沉睡期**：连续超过 90 天无主动交易

#### 2.3.3 APP 埋点数据

**表 6：`app_events`（用户行为埋点表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `event_id` | VARCHAR(32) | 事件 ID | `E000001` |
| `device_id` | VARCHAR(32) | 设备 ID | `DEV_A001` |
| `open_id` | VARCHAR(64) | 微信 OpenID（如有） | `wx_openid_zhang` |
| `event_type` | VARCHAR(32) | 事件类型 | `页面浏览` / `点击` / `搜索` / `停留` / `分享` / `退出` |
| `page_name` | VARCHAR(64) | 页面名称 | `分期计算器` / `权益商城` / `账单详情` / `境外消费专区` |
| `search_keyword` | VARCHAR(128) | 搜索关键词（如有） | `分期费率` / `境外取现` |
| `duration_sec` | INT | 停留时长（秒） | `45` |
| `timestamp` | DATETIME | 事件时间 | `2026-07-10 09:15:00` |
| `app_version` | VARCHAR(16) | APP 版本 | `8.2.1` |

#### 2.3.4 产品与权益数据

**表 7：`product_catalog`（产品目录表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `product_id` | VARCHAR(32) | 产品 ID | `PROD_WHITE001` |
| `product_name` | VARCHAR(64) | 产品名称 | `XX银行经典版白金信用卡` |
| `card_level` | VARCHAR(16) | 卡等级 | `白金卡` |
| `annual_fee` | DECIMAL(8,2) | 年费 | `3600.00` |
| `annual_fee_waiver` | VARCHAR(128) | 年费减免条件 | `年度消费满8万元免年费` |
| `target_income` | VARCHAR(8) | 目标收入等级 | `H` |
| `key_selling_points` | TEXT | 核心卖点（JSON 数组） | `["机场贵宾厅","高额返现","旅行保险"]` |
| `product_doc_path` | VARCHAR(256) | 产品说明书路径 | `product_docs/白金卡权益说明.pdf` |

**表 8：`benefit_catalog`（权益目录表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `benefit_id` | VARCHAR(32) | 权益 ID | `BEN_Lounge_001` |
| `benefit_name` | VARCHAR(64) | 权益名称 | `机场贵宾厅` |
| `benefit_category` | VARCHAR(32) | 权益类别 | `出行` / `生活` / `消费` / `分期` / `保险` |
| `benefit_desc` | TEXT | 权益描述 | `全年无限次使用全球超过1000间机场贵宾厅` |
| `applicable_products` | TEXT | 适用产品列表（JSON 数组） | `["PROD_WHITE001", "PROD_DIAMOND001"]` |

**表 9：`product_benefit_mapping`（产品-权益关联表）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `product_id` | VARCHAR(32) | 产品 ID |
| `benefit_id` | VARCHAR(32) | 权益 ID |

**表 10：`campaign_catalog`（营销活动目录表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `campaign_id` | VARCHAR(32) | 活动 ID | `CAMP_2026_DOUBLE11` |
| `campaign_name` | VARCHAR(128) | 活动名称 | `双十一分期免息大促` |
| `start_date` | DATE | 开始日期 | `2026-11-01` |
| `end_date` | DATE | 结束日期 | `2026-11-11` |
| `target_segment` | TEXT | 目标客群描述（JSON） | `{"age_range":[22,40],"card_level":["金卡","白金卡"]}` |
| `rules` | TEXT | 活动规则（JSON） | `{"分期期数":[3,6,12],"免息期数":3,"适用商户":["指定线上商户"]}` |
| `campaign_poster_path` | VARCHAR(256) | 活动海报路径 | `posters/nov11_promo.png` |

### 2.4 半结构化/非结构化数据

#### 2.4.1 客服 ASR 文本（JSON 格式对话记录）

**文件路径**：`mock_data/unstructured/asr_transcripts/ASR_{call_id}.json`

**数据格式**：

```json
{
  "call_id": "ASR000001",
  "cust_id": "C000001",
  "phone": "138****8888",
  "timestamp": "2026-07-01T14:30:00Z",
  "duration_sec": 320,
  "call_direction": "inbound",
  "call_reason": "账单咨询",
  "transcript": [
    {
      "role": "customer",
      "text": "你好，我这个月账单金额有点高，能帮我查一下分期的手续费吗？"
    },
    {
      "role": "agent",
      "text": "好的张先生，您本期账单是18500元，目前有3期、6期、12期可选，手续费率分别是0.6%/期、0.5%/期、0.45%/期。"
    },
    {
      "role": "customer",
      "text": "那12期的每月要还多少？"
    },
    {
      "role": "agent",
      "text": "12期的话每期本金1541元，手续费约111元，合计每月1652元左右。"
    },
    {
      "role": "customer",
      "text": "行，那就帮我办12期分期吧。"
    }
  ],
  "resolution": "成功办理12期分期",
  "sentiment_label": "neutral_to_satisfied"
}
```

**建议对话场景覆盖**：

| 编号 | 场景 | 数量建议 | 核心意图标签 |
|------|------|---------|------------|
| 1 | 分期费率咨询 | 200~400 条 | 分期/借贷需求 |
| 2 | 账单疑问/投诉 | 100~200 条 | 不满/流失风险 |
| 3 | 额度调整咨询 | 80~150 条 | 额度/升级需求 |
| 4 | 权益使用咨询 | 80~150 条 | 权益/优惠需求 |
| 5 | 境外消费咨询 | 50~100 条 | 跨境/出行需求 |
| 6 | 卡片激活/开户 | 50~100 条 | 新户/激活引导 |
| 7 | 销户咨询 | 30~50 条 | 流失风险 |
| 8 | 其他常规咨询 | 100~200 条 | 一般咨询 |

#### 2.4.2 产品说明书 / 条款 PDF（非结构化文档）

**文件路径**：`mock_data/unstructured/product_docs/`

**建议生成的文档清单**：

```
product_docs/
├── 白金卡权益说明.pdf          # 多页，包含权益表格、使用规则
├── 分期付款条款_2026版.pdf      # 分期费率表、申请条件、违约责任
├── 信用卡章程_2026版.pdf        # 通用章程条款
├── 积分规则说明.pdf             # 积分获取与兑换规则
├── 境外消费指南.pdf             # 境外使用注意事项
├── 保险权益细则.pdf             # 附赠保险的详细条款
├── 新户开卡礼规则.pdf           # 新户优惠活动
└── ...（共 20~30 份）
```

**处理方式**：
- 使用 `pdfplumber` 或 `PyPDF2` 提取文本内容
- 按段落/章节切片（Chunking，每段约 500~1000 tokens）
- 生成 Embedding 向量存入 Milvus

#### 2.4.3 活动海报（图片，含结构化 JSON 描述）

**文件路径**：`mock_data/unstructured/posters/`

```
posters/
├── nov11_promo.png                  # 双十一活动海报
├── nov11_promo.json                 # 海报的结构化描述
├── spring_festival_promo.png        # 春节活动海报
├── spring_festival_promo.json
├── new_user_welcome.png             # 新户欢迎海报
├── new_user_welcome.json
└── ...（共 10~15 组）
```

**JSON 描述格式**：

```json
{
  "image_file": "nov11_promo.png",
  "activity_name": "双十一分期免息大促",
  "campaign_id": "CAMP_2026_DOUBLE11",
  "start_date": "2026-11-01",
  "end_date": "2026-11-11",
  "main_title": "双十一分期免息，最高省500元！",
  "sub_title": "指定商户单笔满3000元享3期免息",
  "visual_description": "红色渐变背景，中央放置金色大字'11.11'，下方展示3期/6期/12期分期选项，底部白色区域为适用商户logo墙",
  "rules_summary": [
    "活动期间在指定线上商户（天猫、京东、拼多多）单笔消费满3000元，享3期免息",
    "单笔消费满5000元，享6期免息",
    "新户首次分期额外返现50元",
    "每人限享一次免息优惠",
    "不可与其他分期优惠叠加使用"
  ],
  "target_segment": "22-40岁金卡及以上持卡人",
  "cta_text": "立即查看我的分期额度"
}
```

### 2.5 多模态数据处理决策表

| 数据类型 | 存储形态 | 是否需多模态 | 本项目的处理方式 |
|---------|---------|:----------:|----------------|
| 客户基础信息/交易流水/账单/CRM/埋点 | CSV / Parquet（表格） | ❌ 否 | 直接读取解析，Pandas/Polars 处理 |
| 客服 ASR 对话 | JSON 文本 | ❌ 否 | NLP 文本解析与意图分类 |
| 产品说明书/条款 PDF | PDF 文档 | ❌ 否 | pdfplumber 提取文字 → Chunk → Embedding |
| 活动海报 | PNG/JPG 图片 | ⚠️ 部分需要 | **方案一（主推）**：JSON 结构化描述 + 图片留档；**方案二（进阶）**：接入 Qwen-VL 做 OCR + 规则提取 |
| 权益图表/表格截图 | PNG/PDF | ⚠️ 部分需要 | 同上，优先用 JSON Schema 描述 |

> 💡 **建议策略**：首期使用 JSON 结构化描述替代图像理解，在核心功能跑通后，作为技术亮点接入 Qwen-VL 多模态模型做真正的图像语义理解。这样既保证交付进度，又展示技术广度。

### 2.6 产品/权益体系设计

**信用卡产品体系（4 级卡等 × 多系列）**：

```
产品线
├── 普卡系列
│   ├── XX银行标准信用卡（普卡）
│   ├── XX银行 YOUNG 卡（普卡，30 岁以下客群）
│   └── XX银行校园信用卡（普卡，在校大学生）
├── 金卡系列
│   ├── XX银行标准信用卡（金卡）
│   ├── XX银行携程旅行信用卡（金卡）
│   └── XX银行京东联名信用卡（金卡）
├── 白金卡系列
│   ├── XX银行经典版白金信用卡
│   ├── XX银行银联白金信用卡
│   └── XX银行全币种白金信用卡（跨境客群）
└── 钻石/无限卡系列
    ├── XX银行钻石信用卡
    └── XX银行无限信用卡
```

**权益池（按类别组织）**：

| 权益类别 | 权益项示例 | 适用卡等 |
|---------|----------|---------|
| **出行** | 机场贵宾厅、接送机服务、境外租车优惠、航班延误险 | 白金及以上 |
| **生活** | 电影票优惠、餐饮折扣、购物返利、生日双倍积分 | 金卡及以上 |
| **消费** | 指定商户返现、境外消费返现、线上支付积分加倍 | 全部 |
| **分期** | 3/6/12 期免息分期、专项分期额度、账单分期优惠费率 | 全部 |
| **保险** | 旅行意外险、购物保障险、航班延误险、行李丢失险 | 白金及以上 |
| **新户** | 开卡礼（刷卡金/积分/实物礼品）、首刷礼、绑卡礼 | 新户专属 |

### 2.7 Mock 数据生成技术方案

**推荐工具链**：

```python
# 核心依赖
import faker          # 生成仿真中文姓名、地址、手机号等
import numpy as np    # 数值分布控制
import pandas as pd   # 数据组织与导出
from datetime import datetime, timedelta

# 生成策略
# 1. 先定义客户群分布（收入、年龄、城市等维度）
# 2. 用 Faker 生成客户基础信息
# 3. 按业务规则生成交易流水（消费金额服从对数正态分布）
# 4. 按场景模板构造 ASR 对话
# 5. 手动编写产品/权益/活动的结构化 JSON
```

**关键约束**：
- 所有个人信息需脱敏（姓名→`张*明`，身份证→`4403****001`，手机号→`138****8888`）
- 所有数据需携带 `timestamp` 和 `cust_id`（或经 OneID 映射的其他 ID）
- 输出格式统一为 UTF-8 编码的 CSV/JSON/Parquet

---

## 三、模块二：OneID 统一融合引擎

### 3.1 问题定义

同一物理客户在不同业务系统中使用不同标识符，需要建立统一的全局唯一 ID，使系统能够跨数据源识别同一客户。

**多 ID 来源示例**：

```
同一客户"张*明"在各系统中的标识：

银行核心系统：   cust_id   = "C000001"
信用卡表：       card_no   = "6225****01", "6225****02"（一人多卡）
CRM 系统：       crm_id    = "CRM000001"
APP 埋点：       device_id = "DEV_A001"（主力手机）
                 device_id = "DEV_B003"（备用机/iPad）
                 open_id   = "wx_openid_zhang"（微信小程序）
客服 ASR：       来电手机号  = "138****8888"
                身份证号    = "4403****001"
```

### 3.2 OneID 映射设计

#### 3.2.1 ID 分级策略

| ID 级别 | 类型 | 确定性 | 说明 |
|:------:|------|:-----:|------|
| **强 ID** | 身份证号 | 100% | 一个身份证号绝对对应唯一自然人，作为 OneID 锚点 |
| **中 ID** | 手机号 | ~95% | 可能换号，但同一时间点唯一 |
| **中 ID** | 卡号 | 100% | 与身份证号通过银行核心系统强关联 |
| **弱 ID** | 设备 ID | ~90% | 可能换手机、多人共用设备 |
| **弱 ID** | OpenID | ~90% | 微信/支付宝账户与自然人基本一一对应 |

#### 3.2.2 数据库表设计

**表 A：`id_mapping`（ID 映射主表）**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `id` | BIGINT | 自增主键 | `1` |
| `oneid` | VARCHAR(32) | 全局唯一客户 ID | `UID000001` |
| `id_type` | VARCHAR(16) | ID 类型 | `id_card` / `phone` / `card_no` / `device_id` / `open_id` / `crm_id` / `cust_id` |
| `id_value` | VARCHAR(128) | ID 值 | `4403****001` |
| `confidence` | DECIMAL(3,2) | 映射置信度 | `1.00` |
| `source_system` | VARCHAR(32) | 来源系统 | `银行核心` / `CRM` / `APP埋点` / `ASR系统` |
| `first_seen` | DATETIME | 首次发现时间 | `2026-06-01 00:00:00` |
| `last_updated` | DATETIME | 最后更新时间 | `2026-07-15 03:00:00` |
| `is_active` | BOOLEAN | 是否仍有效 | `true` |

**表 B：`id_mapping_log`（映射变更日志表）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `log_id` | VARCHAR(32) | 日志 ID |
| `id_value` | VARCHAR(128) | 发生冲突的 ID 值 |
| `old_oneid` | VARCHAR(32) | 原映射 |
| `new_oneid` | VARCHAR(32) | 新映射 |
| `conflict_reason` | VARCHAR(256) | 冲突原因 |
| `resolution` | VARCHAR(32) | 解决方式：`自动合并` / `人工确认` / `保留原映射` |
| `resolved_at` | DATETIME | 解决时间 |

#### 3.2.3 OneID 生成算法

```
OneID 生成与映射算法（确定性、可复现）

输入：一条新的客户记录，包含若干 ID（如 cust_id, phone, id_card, device_id 等）
输出：该客户的 OneID

步骤 1：检查是否包含强 ID（身份证号）
  如果 id_card 已存在于 id_mapping 表中：
    返回已有的 oneid
  如果 id_card 不存在（新客户）：
    分配新 OneID：UID + 自增序号（如 UID000001）
    将 id_card → new_oneid 写入 id_mapping

步骤 2：处理弱 ID（手机号、设备 ID 等）
  对于每个弱 ID：
    如果该弱 ID 已存在于 id_mapping 表中：
      将当前客户关联到已有 oneid
      记录置信度
    如果该弱 ID 不存在：
      将弱 ID → oneid 写入 id_mapping

步骤 3：冲突检测与处理
  如果一个弱 ID 已被映射到某个 oneid_old，
  但通过强 ID 关联到了另一个 oneid_new：
    情况 A：该弱 ID 与 oneid_old 的关联置信度低 → 更新为 oneid_new
    情况 B：置信度高 → 写入 id_mapping_log，标记 pending 人工确认

步骤 4：定期去重合并
  每天凌晨运行去重任务，检查是否有两个 OneID 实际上对应同一客户
  （如两个 OneID 共享同一个身份证号）→ 自动合并
```

#### 3.2.4 API 接口设计

**接口名称**：`OneID 解析`

```
GET /api/v1/oneid/resolve?type={id_type}&value={id_value}

请求示例：
GET /api/v1/oneid/resolve?type=phone&value=138****8888
GET /api/v1/oneid/resolve?type=card_no&value=6225****01
GET /api/v1/oneid/resolve?type=device_id&value=DEV_A001

响应示例：
{
  "oneid": "UID000001",
  "resolved_from": {
    "type": "phone",
    "value": "138****8888"
  },
  "confidence": 0.95,
  "all_known_ids": [
    {"type": "id_card", "value": "4403****001", "confidence": 1.00},
    {"type": "phone", "value": "138****8888", "confidence": 0.95},
    {"type": "card_no", "value": "6225****01", "confidence": 1.00},
    {"type": "card_no", "value": "6225****02", "confidence": 1.00},
    {"type": "device_id", "value": "DEV_A001", "confidence": 0.90},
    {"type": "device_id", "value": "DEV_B003", "confidence": 0.85},
    {"type": "open_id", "value": "wx_openid_zhang", "confidence": 0.90}
  ],
  "customer_name": "张*明",
  "is_verified": true
}
```

---

## 四、模块三：客户画像与动态记忆引擎

### 4.1 两层画像架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    客户画像 = 静态画像 + 动态记忆                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  第一层：静态画像（基本面）                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 特点：变化缓慢，按天（T+1）批量更新                         │    │
│  │ 存储：PostgreSQL（主存储） + Redis（热缓存，TTL=24h）       │    │
│  │ 更新：每日凌晨 03:00 跑批处理刷新                           │    │
│  │                                                          │    │
│  │ 包含字段：                                                 │    │
│  │ · 人口属性：年龄、性别、城市、职业、收入等级、学历            │    │
│  │ · 账户属性：卡等级、授信额度、已用额度、持卡数量、开户时长     │    │
│  │ · 生命周期：新户(0-3月) / 成长期(3-12月) / 成熟期(12-36月)   │    │
│  │             / 沉睡期(>36月且90天无交易)                     │    │
│  │ · 风险标签：当前逾期状态(M0/M1/M2)、历史逾期次数、          │    │
│  │            最低还款频率、套现风险评分                       │    │
│  │ · 价值标签：近12月消费总额、月均消费、单笔最高、            │    │
│  │            分期贡献收入、客户价值等级(LTV)                  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  第二层：动态记忆（行为层）                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ 特点：实时/准实时变化，事件驱动更新                         │    │
│  │ 存储：Redis（热数据）+ Milvus（事件 Embedding 向量）        │    │
│  │ 更新：分窗口（实时/小时/天级）                              │    │
│  │                                                          │    │
│  │ 包含信号：                                                 │    │
│  │ · 消费趋势信号（近N天消费增/降）                            │    │
│  │ · 搜索意图信号（近7天高频搜索词）                           │    │
│  │ · 浏览偏好信号（近30天浏览页面类别 Top5）                   │    │
│  │ · 关键事件信号（大额消费/境外交易/投诉/分期申请）            │    │
│  │ · 活跃度信号（登录频率、交易频率、互动频率）                 │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 动态记忆的多时间窗口设计

| 窗口级别 | 窗口长度 | 聚合粒度 | 更新频率 | 内容示例 | 主要用途 |
|:------:|:------:|:------:|:------:|---------|---------|
| **实时** | ≤1 小时 | 事件级 | 事件触发 | “刚刚搜索了分期”“1 小时前发生境外消费” | B 端实时策略触发、C 端即时个性化 |
| **短期** | 7 天 | 小时级 | 每小时 | “近 7 天消费总额 8500 元”“近 7 天搜索 top5：[出境游, 分期, 汇率]” | 短期意图感知、热点捕捉 |
| **中期** | 30 天 | 天级 | 每日 T+1 | “近 30 天月均消费 12000 元，环比 ↓20%”“浏览偏好：权益商城 > 分期计算器” | 月度画像刷新、趋势判断 |
| **长期** | 90 天 | 天级 | 每日 T+1 | “近 90 天消费总额 45000 元，趋势：↓（降级信号）”“近 90 天活跃度评分：62/100” | 生命周期阶段判定、沉睡预警 |

**时间窗口计算示意**：

```
客户"张*明"的动态记忆演进

Day-90              Day-30            Day-7          Day-1       Now
  │                   │                 │              │          │
  │←── 90天窗口 ────→│                 │              │          │
  │  消费总额: ¥45000                   │              │          │
  │  月均消费: ¥15000                   │              │          │
  │  活跃度: 72/100                     │              │          │
  │                   │←── 30天窗口 ──→│              │          │
  │                   │  月均消费: ¥12000            │          │
  │                   │  趋势: ↓20% (触发降级预警)    │          │
  │                   │  浏览偏好: [权益商城 60%,     │          │
  │                   │    分期计算器 25%]            │          │
  │                   │                 │← 7天窗口 →│          │
  │                   │                 │  搜索: "出境游"×8     │
  │                   │                 │  消费: ¥3200          │
  │                   │                 │              │←实时→│
  │                   │                 │              │ 刚搜索"分期费率"│
  │                   │                 │              │ 刚浏览分期计算器│
```

### 4.3 画像更新触发机制

| 机制 | 触发方式 | 更新内容 | 技术实现 |
|------|---------|---------|---------|
| **定时批处理** | 每日凌晨 03:00 | 静态画像全量刷新、长期/中期窗口聚合、生命周期阶段判定 | Pandas/Polars 批量任务 + APScheduler |
| **微批处理** | 每小时一次 | 短期窗口指标刷新、信号检测（降级/活跃度骤降等） | 轻量聚合任务 + Redis 写入 |
| **事件驱动** | 新事件到达（Kafka 消费） | 实时窗口更新、关键事件推送 | 消息队列消费者 + Redis Pub/Sub |

### 4.4 Redis 缓存 Key 设计

```
# 静态画像缓存（TTL = 24h）
profile:static:{oneid}          → JSON (CustomerProfile 静态部分)

# 动态记忆缓存
memory:realtime:{oneid}         → ZSET  (score=timestamp, member=event_json)  # TTL=2h
memory:short:{oneid}:7d         → JSON  (7天聚合指标)                           # TTL=1h
memory:mid:{oneid}:30d          → JSON  (30天聚合指标)                          # TTL=6h
memory:long:{oneid}:90d         → JSON  (90天聚合指标)                          # TTL=24h

# 事件流
events:{oneid}:timeline         → LIST  (最近100条事件，按时间排序)              # 固定长度
events:{oneid}:milestones       → HASH  (关键里程碑事件)
```

### 4.5 画像变化时的实时调整策略

当客户行为发生显著变化时（例如突然出现降级信号），画像的调整路径如下：

```
事件发生（新交易/新搜索/新客服对话）
    │
    ▼
Kafka 消息队列（实时消费）
    │
    ├─→ 更新 Redis 实时窗口（毫秒级）
    │
    ├─→ 意图评分引擎重新计算（同步）
    │   └─→ 如果意图分数变化超过阈值 → 触发 B 端感知
    │
    ├─→ 检测是否触发预警信号（如降级、沉睡、投诉）
    │   └─→ 如触发 → 写入信号标记到 Redis
    │
    └─→ 下次微批处理/批处理时纳入聚合计算
```

---

## 五、模块四：GraphRAG 企业知识图谱构建

### 5.1 什么是 RAG？

**RAG（Retrieval-Augmented Generation，检索增强生成）** 是一种让大模型能够“查资料”再回答的技术。

传统大模型的问题：它不知道你们银行的白金卡权益是什么，也不知道“张*明”这个客户的具体情况——这些是私有数据，不在模型的训练语料中。

RAG 解决思路：

```
用户提问："白金卡在机场有哪些权益？"
    │
    ▼
① 检索（Retrieval）：把问题转成向量 → 在知识库中搜索相关文档
    │
    ▼
② 找到：《白金卡权益说明.pdf》第3页片段：
   "机场贵宾厅全年无限次使用，覆盖全球超过1000间贵宾厅"
    │
    ▼
③ 增强（Augmented）：把原文 + 问题一起喂给大模型
    │
    ▼
④ 生成（Generation）：大模型基于检索到的资料回答：
   "白金卡持卡人可全年无限次使用全球超过1000间机场贵宾厅..."
```

### 5.2 为什么需要 GraphRAG？

**传统 RAG 的局限**：

如果问“张*明最近为什么收到这么多分期推送？”，普通 RAG 只能：
- 搜索到“分期条款 PDF”的片段
- 但它不知道“张*明这个人是谁”
- 不知道他持有什么卡
- 不知道他最近消费了多少
- 不知道他最近搜索了“分期费率”

因为这些信息**分散在不同的表、不同的文档里，没有关联索引**。

**GraphRAG 的改进**：

GraphRAG = **知识图谱（Graph）+ RAG**。知识图谱把**实体**（客户、产品、权益、活动）和**关系**（持有、关联、参与、适用）连接成一张网：

```
                     ┌──────────────────┐
                     │  客户：张*明       │
                     │  OneID: UID000001 │
                     │  年龄: 32 | 深圳   │
                     │  生命周期: 成熟期   │
                     └──┬──────────┬────┘
                        │          │
             [持有]     │          │    [参与]
                        │          │
        ┌───────────────▼──┐   ┌──▼────────────────────┐
        │ 产品：白金信用卡    │   │ 活动：双十一分期大促     │
        │ 额度: ¥200,000    │   │ 时间: 2026.11.01-11.11 │
        │ 开卡: 2020-03     │   │ 规则: 满3000享3期免息   │
        └───────┬──────────┘   └───────────┬────────────┘
                │                          │
       [关联]   │                          │ [适用产品]
                │                          │
        ┌───────▼──────────────┐           │
        │ 权益包：白金权益       │           │
        │ · 机场贵宾厅（全年无限）│           │
        │ · 接送机服务（6次/年） │           │
        │ · 酒店升级（2次/年）   │           │
        │ · 航班延误险          │           │
        └──────────────────────┘           │
                                           │
                        ┌──────────────────▼──────────┐
                        │ 条款：分期付款条款 2026 版      │
                        │ · 3期手续费 0.6%/期           │
                        │ · 6期手续费 0.5%/期           │
                        │ · 12期手续费 0.45%/期         │
                        └─────────────────────────────┘
```

### 5.3 GraphRAG 检索流程（四步）

```
用户问："张*明适合推荐什么分期方案？"
    │
    ▼
┌─ 第一步：图谱检索（Neo4j Cypher 查询）─────────────────────┐
│                                                           │
│  MATCH (c:Customer {oneid:'UID000001'})-[r]-(n) RETURN *   │
│                                                           │
│  从"张*明"节点出发，沿关系遍历获取：                         │
│  · 张*明 → [持有] → 白金信用卡 (额度200k)                   │
│  · 张*明 → [参与] → 双十一分期活动                         │
│  · 白金信用卡 → [关联] → 白金权益包                        │
│  · 最新账单：¥18,500（来自 profile 侧）                    │
│                                                           │
│  图谱返回：他是白金卡客户，当前账单较高，在活动覆盖范围内      │
└───────────────────────────────────────────────────────────┘
    │
    ▼
┌─ 第二步：向量检索（Milvus）────────────────────────────────┐
│                                                           │
│  将"分期方案推荐"转为 Embedding 向量                         │
│  → 在 Milvus 中搜索 Top-K 相似文档片段                      │
│                                                           │
│  返回：                                                    │
│  · 《分期条款 2026 版》片段："12期手续费 0.45%/期..."       │
│  · 《双十一活动规则》片段："满3000享3期免息..."             │
│  · 《白金卡权益》片段："白金卡专享分期优惠..."              │
└───────────────────────────────────────────────────────────┘
    │
    ▼
┌─ 第三步：融合排序 ────────────────────────────────────────┐
│                                                           │
│  对图谱结果和向量结果进行融合与重排序：                       │
│  · 相关性打分（与问题的语义相似度）                          │
│  · 新鲜度加权（活动是否在有效期内）                          │
│  · 权威性加权（条款 > 活动海报 > FAQ）                      │
│  · 去重合并                                               │
│                                                           │
│  → 输出融合后的 Top-K 上下文片段                            │
└───────────────────────────────────────────────────────────┘
    │
    ▼
┌─ 第四步：生成答案（大模型）────────────────────────────────┐
│                                                           │
│  输入 = 问题 + 融合上下文 + 客户画像                         │
│                                                           │
│  大模型（Qwen3-8B）生成：                                   │
│                                                           │
│  "张*明先生是白金卡持卡人，本期账单为 ¥18,500。             │
│   当前推荐 12 期分期方案，手续费仅 0.45%/期，                │
│   每期约还 ¥1,652。同时您正处于双十一活动覆盖范围内，        │
│   可享前 3 期免息优惠，预估可节省手续费约 ¥333。"            │
└───────────────────────────────────────────────────────────┘
```

### 5.4 知识图谱 Schema 设计

#### 5.4.1 实体节点类型（Node Labels）

| 节点类型 | Label | 主要属性 | 数量预估 |
|:------:|------|---------|:------:|
| 客户 | `Customer` | oneid, name, age, gender, city, income_level, lifecycle_stage | 5,000~10,000 |
| 信用卡 | `CreditCard` | card_no, card_level, credit_amount, open_date | 8,000~15,000 |
| 产品 | `Product` | product_id, product_name, card_level, annual_fee | 8~12 |
| 权益 | `Benefit` | benefit_id, benefit_name, benefit_category, description | 50~100 |
| 权益包 | `BenefitPackage` | package_id, package_name | 8~12 |
| 营销活动 | `Campaign` | campaign_id, campaign_name, start_date, end_date, rules | 20~30 |
| 分期条款 | `InstallmentRule` | rule_id, periods, fee_rate, conditions | 10~20 |
| 交易 | `Transaction` | txn_id, amount, merchant_category, is_cross_border, timestamp | 代表性节点 |
| 客服工单 | `ServiceTicket` | call_id, call_reason, resolution, sentiment | 代表性节点 |
| 文档 | `Document` | doc_id, doc_type, title, file_path, chunk_count | 20~50 |

#### 5.4.2 关系类型（Relationship Types）

| 关系类型 | 方向 | 说明 | 示例 |
|:------:|------|------|------|
| `[:HOLDS]` | Customer → CreditCard | 客户持有信用卡 | 张*明 → 白金信用卡 |
| `[:BELONGS_TO]` | CreditCard → Product | 卡片属于某产品 | 白金信用卡 → 经典版白金卡 |
| `[:INCLUDES]` | BenefitPackage → Benefit | 权益包包含权益 | 白金权益包 → 机场贵宾厅 |
| `[:BUNDLED_WITH]` | Product → BenefitPackage | 产品捆绑权益包 | 经典版白金卡 → 白金权益包 |
| `[:PARTICIPATES_IN]` | Customer → Campaign | 客户参与活动 | 张*明 → 双十一活动 |
| `[:APPLIES_TO]` | Campaign → Product | 活动适用产品 | 双十一活动 → 经典版白金卡 |
| `[:GOVERNED_BY]` | Product → InstallmentRule | 产品受条款约束 | 经典版白金卡 → 分期条款 |
| `[:MADE]` | Customer → Transaction | 客户产生交易 | 张*明 → 交易 T000001 |
| `[:MADE_ON]` | Transaction → CreditCard | 交易发生在卡上 | T000001 → 白金信用卡 |
| `[:REQUESTED]` | Customer → ServiceTicket | 客户发起工单 | 张*明 → ASR000001 |
| `[:RELATES_TO]` | Document → Product/Benefit/Campaign | 文档关联实体 | 权益说明.pdf → 经典版白金卡 |
| `[:SIMILAR_TO]` | Customer → Customer | 客户相似度 | 张*明 --[相似]--> 李*芳 |

#### 5.4.3 Neo4j Cypher 建图脚本（示例）

```cypher
// 创建约束
CREATE CONSTRAINT IF NOT EXISTS FOR (c:Customer) REQUIRE c.oneid IS UNIQUE;
CREATE CONSTRAINT IF NOT EXISTS FOR (p:Product) REQUIRE p.product_id IS UNIQUE;

// 创建客户节点
CREATE (:Customer {
  oneid: 'UID000001',
  name: '张*明',
  age: 32,
  gender: 'M',
  city: '深圳',
  income_level: 'H',
  lifecycle_stage: '成熟期'
});

// 创建产品节点
CREATE (:Product {
  product_id: 'PROD_WHITE001',
  product_name: 'XX银行经典版白金信用卡',
  card_level: '白金卡',
  annual_fee: 3600.00
});

// 创建关系
MATCH (c:Customer {oneid:'UID000001'}), (p:Product {product_id:'PROD_WHITE001'})
CREATE (c)-[:HOLDS {card_no:'6225****01', credit_amount:200000, open_date:date('2020-03-20')}]->(p);
```

### 5.5 非结构化文档处理流水线

```
┌─────────────────────────────────────────────────────────────┐
│              文档处理流水线（Document Ingestion Pipeline）       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  PDF / Word / Markdown                                       │
│       │                                                      │
│       ▼                                                      │
│  ┌──────────────┐                                           │
│  │ ① 文档解析    │  pdfplumber/PyPDF2/python-docx             │
│  │   提取纯文本   │  去除页眉页脚、保留段落结构                  │
│  └──────┬───────┘                                           │
│         │                                                    │
│         ▼                                                    │
│  ┌──────────────┐                                           │
│  │ ② 文本切片    │  LangChain RecursiveCharacterTextSplitter   │
│  │   Chunking   │  chunk_size=800, overlap=100               │
│  │              │  按段落边界智能切分                           │
│  └──────┬───────┘                                           │
│         │                                                    │
│         ▼                                                    │
│  ┌──────────────┐                                           │
│  │ ③ 向量化      │  Qwen3-Embedding-0.6B / BGE-M3            │
│  │   Embedding  │  文本 → 1024 维向量                         │
│  └──────┬───────┘                                           │
│         │                                                    │
│         ▼                                                    │
│  ┌──────────────┐                                           │
│  │ ④ 向量存储    │  Milvus Collection                         │
│  │              │  存储字段: chunk_id, text, embedding,       │
│  │              │           doc_id, page_num, chunk_index     │
│  └──────┬───────┘                                           │
│         │                                                    │
│         ▼                                                    │
│  ┌──────────────┐                                           │
│  │ ⑤ 图谱关联    │  在 Neo4j 创建 Document 节点                │
│  │              │  建立 Document → Product/Benefit 关系       │
│  └──────────────┘                                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 5.6 GraphRAG 系统架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                   GraphRAG 系统架构                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────────┐       ┌─────────────────┐             │
│   │     Neo4j        │       │     Milvus      │             │
│   │   (图数据库)      │       │   (向量数据库)    │             │
│   │                  │       │                 │             │
│   │  存储内容：       │       │  存储内容：       │             │
│   │  · 实体节点       │       │  · 文档片段向量    │             │
│   │  · 关系边        │       │  · 事件向量      │             │
│   │  · 图谱遍历       │       │  · FAQ 向量      │             │
│   │                  │       │                 │             │
│   │  查询语言：       │       │  维度：1024       │             │
│   │  Cypher          │       │  模型：Qwen-Emb  │             │
│   └────────┬────────┘       └────────┬────────┘             │
│            │                         │                       │
│            └──────────┬──────────────┘                       │
│                       │                                      │
│              ┌────────▼──────────┐                           │
│              │  GraphRAG 检索层   │                           │
│              │                   │                           │
│              │  ① 意图解析       │                           │
│              │  ② 图谱查询       │                           │
│              │     (Cypher)      │                           │
│              │  ③ 向量相似检索    │                           │
│              │     (Milvus)      │                           │
│              │  ④ 融合排序       │                           │
│              │     (Reranker)    │                           │
│              │  ⑤ 上下文组装     │                           │
│              └────────┬──────────┘                           │
│                       │                                      │
│              ┌────────▼──────────┐                           │
│              │   统一 API         │                           │
│              │   /knowledge/      │                           │
│              │   search           │                           │
│              └───────────────────┘                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 六、模块五：智能意图识别与非结构化洞察

### 6.1 功能定位

智能意图识别引擎是系统面向 B 端策略大脑的**第一手信号输入源**。

**核心定位**：

```
你的职责（A 端）                          B 端的职责
────────────────                        ────────────
识别"客户现在想要什么"          ─→       根据意图决定"推什么策略"
输出意图向量（IntentVector）    ─→       读取意图向量做策略决策

你负责"认"（感知客户意图）       B 负责"用"（基于意图做策略编排）
```

你不需要关心 B 端如何根据意图推策略。你只需要告诉 B 端：

> “这个客户当前有 85% 的概率想要分期，同时有 40% 的概率对出境游感兴趣，整体情绪偏焦虑。”

剩下的是 B 端的事。

### 6.2 意图分类体系（6 大类）

#### 完整意图分类表

```
┌─────────────────────────────────────────────────────────────────┐
│  意图大类     │  子场景 / 触发信号               │  B 端可用的策略方向  │
├─────────────────────────────────────────────────────────────────┤
│               │ · 账单金额 > 额度的 70%          │  推送分期免息优惠   │
│               │ · 近7天搜索"分期""手续费"≥2次    │  推送专项分期额度    │
│  ① 分期/借贷  │ · 客服通话中提到"账单压力"       │  推最低还款转分期    │
│     需求       │ · 近3个月有最低还款记录          │  推送账单分期方案    │
│               │ · 浏览"分期计算器"页面≥3次       │                    │
│               │ · 大额消费后未全额还款            │                    │
├─────────────────────────────────────────────────────────────────┤
│               │ · 近30天境外交易≥3笔              │  推送境外返现卡     │
│               │ · 近7天搜索"境外""汇率""海淘"     │  推送旅行保险       │
│  ② 跨境/出行  │ · 购买国际机票/酒店              │  推送机场贵宾厅权益  │
│     需求       │ · 搜索旅游目的地（如"东京""巴黎"）│  推送接送机服务      │
│               │ · 持卡有境外返现权益但未使用      │  推送汇率优惠       │
│               │ · 境外取现行为                    │                    │
├─────────────────────────────────────────────────────────────────┤
│               │ · 近90天额度使用率 > 85%          │  推送提额邀请       │
│               │ · 近7天搜索"提额""额度不够"       │  推送高端卡升级      │
│  ③ 额度/升级  │ · 收入等级提升但额度未更新        │  推送临时额度       │
│     需求       │ · 浏览高端卡产品页面≥3次         │  推送白金卡邀请      │
│               │ · 大额消费被拒付                  │                    │
├─────────────────────────────────────────────────────────────────┤
│               │ · 近30天积分即将过期              │  推送积分兑换活动    │
│               │ · 近7天浏览权益商城≥5次           │  推送限时优惠券      │
│  ④ 权益/优惠  │ · 搜索"积分兑换""里程""优惠券"    │  推送生日月双倍积分   │
│     需求       │ · 生日月临近（本月或下月）        │  推送合作商户折扣    │
│               │ · 浏览特定商户页面                │  推送权益使用提醒    │
├─────────────────────────────────────────────────────────────────┤
│               │ · 连续90天无主动交易               │  推送唤醒红包       │
│               │ · 额度使用率持续下降超过30天       │  推送0门槛优惠       │
│  ⑤ 沉睡/流失  │ · 客服投诉/不满表达               │  推送客户关怀        │
│     风险       │ · 竞品活动期间活跃度低            │  推送专属客户经理    │
│               │ · 搜索"销户""注销"               │  推送高价值权益      │
│               │ · 连续多张卡处于休眠状态           │                    │
├─────────────────────────────────────────────────────────────────┤
│               │ · 新户首月活跃但未二次消费          │  推送新手权益       │
│               │ · 首刷后超过7天未再使用            │  推送首刷礼         │
│  ⑥ 新户/激活  │ · 仅开通未激活（静卡）            │  推送激活引导       │
│     引导       │ · 绑定快捷支付但未产生交易         │  推送首笔消费返现    │
│               │ · 浏览新手引导页面                 │  推送新户专属分期    │
└─────────────────────────────────────────────────────────────────┘
```

> 详细意图定义、触发规则与评分权重表见附录 B。

### 6.3 意图识别引擎架构

```
┌──────────────────────────────────────────────────────────────┐
│                   意图识别引擎架构                              │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  输入信号源：                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ 交易流水  │ │ APP 埋点  │ │ 客服 ASR  │ │ 搜索记录  │        │
│  │ (金额/    │ │ (浏览/    │ │ (对话文本) │ │ (关键词)  │        │
│  │  商户/    │ │  点击/    │ │            │ │          │        │
│  │  渠道)   │ │  搜索)   │ │            │ │          │        │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘        │
│       └────────────┴────────────┴────────────┘               │
│                         │                                     │
│                         ▼                                     │
│  ┌──────────────────────────────────────────────────┐       │
│  │           第一层：规则评分引擎（Rule-based Scoring）│       │
│  │                                                   │       │
│  │  技术路线：纯 Python 规则引擎                       │       │
│  │  特点：可控、可解释、低延迟、实时运行                │       │
│  │                                                   │       │
│  │  每条意图定义一组触发规则 + 权重，加权求和：          │       │
│  │                                                   │       │
│  │  分期需求评分 =                                    │       │
│  │    (账单/额度比 > 70%      → +30 分)              │       │
│  │    + (近7天搜索"分期"×N    → +20 分)              │       │
│  │    + (客服提到"账单压力"    → +25 分)              │       │
│  │    + (近3月有最低还款      → +15 分)              │       │
│  │    + (浏览分期计算器≥3次   → +10 分)              │       │
│  │    ──────────────────────────────────             │       │
│  │    → 总分 85 → 高置信度分期意图！                   │       │
│  │                                                   │       │
│  └──────────────────────┬───────────────────────────┘       │
│                         │                                     │
│                         ▼  (对于 ASR 文本等需深度理解的信号)     │
│  ┌──────────────────────────────────────────────────┐       │
│  │         第二层：大模型语义理解（LLM Classifier）     │       │
│  │                                                   │       │
│  │  技术路线：Qwen3-8B（开源/API 调用）                │       │
│  │  特点：理解复杂语义、识别隐含意图、情感分析          │       │
│  │                                                   │       │
│  │  输入：ASR 对话全文                                │       │
│  │  提示词模板：                                       │       │
│  │  "分析以下银行客服通话记录，识别客户的潜在意图和      │       │
│  │   情感状态。请以JSON格式返回..."                   │       │
│  │                                                   │       │
│  │  输出示例：                                        │       │
│  │  {                                                │       │
│  │    "primary_intent": "分期需求",                   │       │
│  │    "intent_score": 85,                            │       │
│  │    "sentiment": "焦虑",                            │       │
│  │    "urgency": "高",                                │       │
│  │    "key_phrases": ["账单太高","可能还不上","12期"]  │       │
│  │  }                                                │       │
│  └──────────────────────┬───────────────────────────┘       │
│                         │                                     │
│                         ▼                                     │
│  ┌──────────────────────────────────────────────────┐       │
│  │             第三层：结果融合与输出                    │       │
│  │                                                   │       │
│  │  规则评分结果 + LLM 语义分析结果 → 加权融合         │       │
│  │  → 输出标准 IntentVector JSON                      │       │
│  └──────────────────────────────────────────────────┘       │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 6.4 情感/情绪分析维度

除了“意图”，引擎还输出**情感（情绪）维度**，帮助 B 端调整推送话术：

| 情感标签 | 典型触发信号 | 对 B 端策略的影响 |
|---------|------------|-----------------|
| **满意** | 客服评价高、持续活跃、主动推荐他人 | 可推送升级/高端权益，话术可积极 |
| **中性** | 日常正常使用，无明显情绪表达 | 按标准策略执行 |
| **焦虑** | 通话中表达“还不上”“压力大”、多次查看账单 | 优先推荐低门槛分期，话术需温和、关怀 |
| **不满** | 投诉记录、APP 频繁跳出、搜索“投诉”“消保” | 避免频繁营销，先推送安抚/补偿方案 |
| **好奇** | 多次浏览新产品页面、搜索产品对比 | 推送详细介绍、对比优势，引导转化 |

### 6.5 意图识别 API 输出契约

```json
// GET /api/v1/customer/{oneid}/intent
{
  "oneid": "UID000001",
  "updated_at": "2026-07-15T14:30:00Z",
  "intents": [
    {
      "type": "分期/借贷需求",
      "score": 85,
      "confidence": "high",
      "sub_signals": [
        {"signal": "账单/额度比", "value": "78%", "weight": 30, "threshold": ">70%"},
        {"signal": "近7天搜索分期关键词", "value": "3次", "weight": 20, "threshold": "≥2次"},
        {"signal": "近7天客服咨询分期", "value": "1次", "weight": 25, "threshold": "≥1次"},
        {"signal": "近3月最低还款次数", "value": "2次", "weight": 10, "threshold": "≥1次"},
        {"signal": "近7天浏览分期计算器", "value": "4次", "weight": 15, "threshold": "≥3次"}
      ],
      "trend": "rising",
      "evidence_summary": "客户近30天消费占额度78%，近期多次搜索分期费率并致电客服咨询分期方案，已触发强烈分期需求信号"
    },
    {
      "type": "跨境/出行需求",
      "score": 45,
      "confidence": "medium",
      "sub_signals": [...],
      "trend": "rising"
    },
    {
      "type": "权益/优惠需求",
      "score": 30,
      "confidence": "low",
      "sub_signals": [...],
      "trend": "stable"
    }
  ],
  "primary_intent": "分期/借贷需求",
  "sentiment": {
    "overall": "anxious",
    "confidence": 0.78,
    "anxiety_score": 65,
    "satisfaction_score": 40,
    "key_evidence": "客服通话中多次表达'账单太高''压力大'等焦虑信号"
  }
}
```

---

## 七、模块六：统一 API 服务层

### 7.1 设计目标

通过 FastAPI 构建统一的 REST API 服务层，对外（B 端、C 端）提供标准化的数据供给接口。

### 7.2 核心 API 清单

| 编号 | 接口路径 | 方法 | 功能说明 | 消费方 |
|:--:|---------|:--:|---------|:----:|
| API-01 | `/api/v1/oneid/resolve` | GET | OneID 解析（根据任意 ID 查 OneID） | B, C |
| API-02 | `/api/v1/customer/{oneid}/profile` | GET | 获取客户完整画像（静态+动态） | B, C |
| API-03 | `/api/v1/customer/{oneid}/events` | GET | 获取客户事件流（支持时间窗口参数） | B |
| API-04 | `/api/v1/customer/{oneid}/intent` | GET | 获取客户当前意图向量 | B |
| API-05 | `/api/v1/knowledge/search` | POST | GraphRAG 知识检索（图谱+向量） | B, C |
| API-06 | `/api/v1/customer/{oneid}/graph` | GET | 获取客户知识图谱子图 | 大屏, B |
| API-07 | `/api/v1/customer/search` | POST | 多条件客户搜索与圈选 | 大屏, B |
| API-08 | `/api/v1/feedback/update` | POST | 接收 C 端回传的行为反馈数据 | C→A |
| API-09 | `/api/v1/health` | GET | 服务健康检查 | 运维 |

### 7.3 接口详细定义

#### API-01: OneID 解析

```
GET /api/v1/oneid/resolve?type=phone&value=138****8888

Response 200:
{
  "oneid": "UID000001",
  "resolved_from": {"type": "phone", "value": "138****8888"},
  "confidence": 0.95,
  "all_known_ids": [...]
}
```

#### API-02: 客户完整画像

```
GET /api/v1/customer/UID000001/profile

Response 200:
{
  "oneid": "UID000001",
  "static_profile": {
    "name": "张*明",
    "gender": "M",
    "age": 32,
    "city": "深圳",
    "occupation": "IT/互联网",
    "income_level": "H",
    "lifecycle_stage": "成熟期",
    "cards": [
      {
        "card_no": "6225****01",
        "card_level": "白金卡",
        "credit_amount": 200000,
        "used_amount": 156000,
        "usage_rate": 0.78,
        "open_date": "2020-03-20",
        "product_name": "经典版白金信用卡"
      }
    ],
    "risk_tags": {
      "overdue_status": "M0",
      "history_overdue_count": 1,
      "min_payment_frequency_3m": 2
    },
    "value_tags": {
      "annual_consumption": 180000,
      "monthly_avg_consumption": 15000,
      "ltv_level": "高",
      "installment_contribution_12m": 1200
    }
  },
  "dynamic_memory": {
    "realtime_signals": [
      {"event": "搜索", "detail": "分期费率", "timestamp": "2026-07-15T14:20:00Z"}
    ],
    "short_term_7d": {
      "total_consumption": 8500,
      "top_search_keywords": ["出境游", "分期", "汇率"],
      "active_days": 6
    },
    "mid_term_30d": {
      "total_consumption": 12000,
      "consumption_trend": "down",
      "trend_change_pct": -20,
      "top_browse_categories": [
        {"category": "权益商城", "ratio": 0.60},
        {"category": "分期计算器", "ratio": 0.25}
      ]
    },
    "long_term_90d": {
      "total_consumption": 45000,
      "consumption_trend": "down",
      "activity_score": 62,
      "dormancy_risk": "medium"
    }
  },
  "generated_at": "2026-07-15T15:00:00Z"
}
```

#### API-03: 客户事件流

```
GET /api/v1/customer/UID000001/events?window=30d&event_type=all&limit=50

Query Parameters:
  - window: 7d | 30d | 90d  (默认 30d)
  - event_type: all | transaction | search | browse | service | campaign
  - limit: 1-200 (默认 50)

Response 200:
{
  "oneid": "UID000001",
  "window": "30d",
  "total_events": 128,
  "events": [
    {
      "event_id": "E000100",
      "event_type": "search",
      "detail": {"keyword": "分期费率", "page": "搜索页"},
      "timestamp": "2026-07-15T14:20:00Z"
    },
    {
      "event_id": "T000050",
      "event_type": "transaction",
      "detail": {"amount": 3500, "merchant": "Apple US", "is_cross_border": true},
      "timestamp": "2026-07-14T10:30:00Z"
    }
  ]
}
```

#### API-04: 客户意图向量

```
GET /api/v1/customer/UID000001/intent

Response 200:
{
  "oneid": "UID000001",
  "updated_at": "2026-07-15T14:30:00Z",
  "primary_intent": "分期/借贷需求",
  "intents": [
    {"type": "分期/借贷需求", "score": 85, "confidence": "high", "trend": "rising"},
    {"type": "跨境/出行需求", "score": 45, "confidence": "medium", "trend": "rising"},
    {"type": "权益/优惠需求", "score": 30, "confidence": "low", "trend": "stable"}
  ],
  "sentiment": {
    "overall": "anxious",
    "anxiety_score": 65,
    "satisfaction_score": 40
  }
}
```

#### API-05: GraphRAG 知识检索

```
POST /api/v1/knowledge/search

Request Body:
{
  "query": "白金卡有哪些机场权益？",
  "customer_id": "UID000001",          // 可选，带入客户上下文做个性化检索
  "top_k": 5,
  "include_graph": true,               // 是否包含图谱检索结果
  "include_vector": true               // 是否包含向量检索结果
}

Response 200:
{
  "query": "白金卡有哪些机场权益？",
  "results": [
    {
      "rank": 1,
      "source_type": "graph",
      "content": "经典版白金信用卡 → 捆绑 → 白金权益包 → 包含 → 机场贵宾厅（全年无限次）",
      "source_doc": "白金卡权益说明.pdf",
      "relevance_score": 0.95
    },
    {
      "rank": 2,
      "source_type": "vector",
      "content": "机场贵宾厅服务覆盖全球超过1000间机场贵宾厅，包括深圳宝安、北京首都、上海浦东、广州白云等主要机场。持卡人可提前通过手机银行APP预约...",
      "source_doc": "白金卡权益说明.pdf",
      "page": 3,
      "relevance_score": 0.92
    }
  ],
  "graph_context": {
    "related_entities": [
      {"type": "Product", "name": "经典版白金信用卡"},
      {"type": "BenefitPackage", "name": "白金权益包"},
      {"type": "Benefit", "name": "机场贵宾厅"}
    ]
  }
}
```

#### API-06: 客户知识图谱子图

```
GET /api/v1/customer/UID000001/graph?depth=2

Response 200:
{
  "oneid": "UID000001",
  "nodes": [
    {"id": "UID000001", "label": "Customer", "name": "张*明", "group": "customer"},
    {"id": "PROD_WHITE001", "label": "Product", "name": "经典版白金信用卡", "group": "product"},
    {"id": "BEN_Lounge_001", "label": "Benefit", "name": "机场贵宾厅", "group": "benefit"},
    ...
  ],
  "edges": [
    {"from": "UID000001", "to": "PROD_WHITE001", "label": "持有"},
    {"from": "PROD_WHITE001", "to": "BEN_Lounge_001", "label": "关联"},
    ...
  ]
}
```

#### API-07: 客户搜索与圈选

```
POST /api/v1/customer/search

Request Body:
{
  "filters": {
    "age_range": [25, 40],
    "city": ["深圳", "上海", "北京"],
    "card_level": ["白金卡", "钻石卡"],
    "lifecycle_stage": ["成熟期"],
    "income_level": ["H", "M"],
    "intent": {"type": "分期/借贷需求", "score_min": 60}
  },
  "pagination": {"page": 1, "page_size": 20}
}

Response 200:
{
  "total_count": 156,
  "page": 1,
  "page_size": 20,
  "customers": [
    {
      "oneid": "UID000001",
      "name": "张*明",
      "city": "深圳",
      "card_level": "白金卡",
      "primary_intent": "分期/借贷需求",
      "intent_score": 85
    }
  ]
}
```

#### API-08: 反馈数据回传（C → A）

```
POST /api/v1/feedback/update

Request Body:
{
  "oneid": "UID000001",
  "campaign_id": "CAMP_2026_DOUBLE11",
  "feedback_type": "conversion",
  "metrics": {
    "impression": true,
    "click": true,
    "conversion": true,
    "conversion_amount": 12500,
    "channel": "APP Push",
    "response_time_sec": 3600
  },
  "timestamp": "2026-07-15T16:00:00Z"
}

Response 202: {"status": "accepted", "message": "Feedback queued for processing"}
```

### 7.4 技术实现要点

```python
# FastAPI 入口示例结构
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from routers import oneid, customer, knowledge, feedback

app = FastAPI(
    title="Knowledge Agent API",
    description="企业知识引擎与记忆中心 - 统一数据服务",
    version="1.0.0"
)

app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])

app.include_router(oneid.router, prefix="/api/v1/oneid", tags=["OneID"])
app.include_router(customer.router, prefix="/api/v1/customer", tags=["Customer"])
app.include_router(knowledge.router, prefix="/api/v1/knowledge", tags=["Knowledge"])
app.include_router(feedback.router, prefix="/api/v1/feedback", tags=["Feedback"])
```

**关键非功能需求**：

| 需求 | 策略 |
|------|------|
| **缓存** | Redis 缓存热点数据，TTL 按数据新鲜度设定（静态画像 24h，动态记忆 1h，意图 5min） |
| **限流** | 每 API 端点设置 Rate Limiting（如 100 req/min per IP） |
| **日志** | 结构化日志（JSON 格式），包含 request_id 全链路追踪 |
| **文档** | FastAPI 自动生成 Swagger UI（`/docs`）和 ReDoc（`/redoc`） |
| **健康检查** | `/api/v1/health` 返回各依赖服务（Neo4j, Milvus, Redis）的连接状态 |

---

## 八、模块七：数据基座大屏与数据管理页面

### 8.1 页面总体设计

需要提供 **两个页面 + 一个 API 文档页**：

| 页面 | 路由 | 功能 | 面向角色 |
|------|------|------|:------:|
| **客户 360 视图大屏** | `/dashboard/customer/{oneid}` | 搜索客户 → 展示画像+知识图谱+意图走势+事件时间线 | 运营人员 / 答辩演示 |
| **数据导入管理页** | `/admin/data-import` | 上传不同格式数据文件，选择处理方式（OCR/Qwen-VL/PDF 解析），查看处理历史 | 数据管理员 |
| **API 文档页** | `/docs` | FastAPI 自动生成的 Swagger UI，供 B/C 端调试调用 | B/C 端开发者 |

### 8.2 页面一：客户 360 视图大屏

**页面布局**：

```
┌──────────────────────────────────────────────────────────────────┐
│  🏦 XX银行信用卡 AI 营销中枢 · Knowledge Agent         [Admin]  │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  🔍 [输入客户 ID / 姓名 / 手机号 搜索...                    ] 🔍  │
│     [按 OneID] [按姓名] [按手机号] [按身份证号]                    │
│                                                                   │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────┐  │
│  │  基本信息      │  │  意图雷达图    │  │  情感/活跃度仪表盘      │  │
│  │              │  │              │  │                        │  │
│  │  张*明       │  │     分期      │  │  活跃度 ████████░░ 82%  │  │
│  │  男 | 32岁   │  │    ↗   ↘     │  │  满意度 ██████░░░░ 65%  │  │
│  │  深圳        │  │  出行 ← → 升级│  │  焦虑度 ███░░░░░░░ 35%  │  │
│  │  白金卡持卡人 │  │    ↖   ↗     │  │                        │  │
│  │  额度: ¥200k │  │     权益      │  │  生命周期: 成熟期        │  │
│  │  [查看详情]   │  │              │  │                        │  │
│  └──────────────┘  └──────────────┘  └────────────────────────┘  │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │              知识图谱关系网络 (Neo4j 力导向图)                │  │
│  │                                                             │  │
│  │     [张*明] ───[持有]───→ [白金信用卡]                       │  │
│  │        │                     │                              │  │
│  │    [参与]                [关联]                              │  │
│  │        │                     │                              │  │
│  │   [双十一活动]         [白金权益包]                            │  │
│  │                             │                              │  │
│  │              ┌──────────────┼──────────────┐                │  │
│  │          [机场贵宾厅]  [接送机服务]  [航班延误险]              │  │
│  │                                                             │  │
│  │  [点击节点查看详情] [展开全部关联] [按关系类型筛选 ▾]          │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │              动态行为时间线（近期关键事件）                      │  │
│  │                                                             │  │
│  │  07/01    07/07      07/12      07/14      07/15  (Now)    │  │
│  │    ●────────●─────────●──────────●──────────●               │  │
│  │    │        │         │          │          │               │  │
│  │  大额消费  搜索"出境游" 浏览权益商城 境外消费   搜索"分期费率"  │  │
│  │  ¥5,000    ×3次      ×5次       $500      ×2次            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────┐  ┌──────────────────────────────────┐  │
│  │  画像数据详情 (JSON)   │  │  关联知识库 (GraphRAG 检索结果)    │  │
│  │                      │  │                                  │  │
│  │  {                   │  │  📄 白金卡权益说明.pdf             │  │
│  │   "oneid":"UID...",  │  │     匹配片段: "...机场贵宾厅..."   │  │
│  │   "static":{...},    │  │  📄 分期条款 2026 版.pdf           │  │
│  │   "dynamic":{...},   │  │     匹配片段: "...12期手续费..."   │  │
│  │   "intents":[...]    │  │  🖼️ 双十一活动海报.png            │  │
│  │  }                   │  │     解析结果: "3期免息..."        │  │
│  └──────────────────────┘  └──────────────────────────────────┘  │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

**交互功能**：

| 交互 | 说明 |
|------|------|
| 客户搜索 | 支持 OneID / 姓名 / 手机号 / 身份证号 四种方式搜索；支持模糊匹配 |
| 图谱交互 | 节点悬停显示详情 Tooltip；节点点击高亮并展示关联路径；拖动/缩放 |
| 意图雷达图 | 雷达图展示 6 类意图分值；鼠标悬停显示 evidence 摘要 |
| 时间线筛选 | 可按事件类型筛选（消费/搜索/浏览/客服） |
| JSON 数据导出 | 一键复制或下载完整画像 JSON |

### 8.3 页面二：数据导入管理页

**页面布局**：

```
┌──────────────────────────────────────────────────────────────────┐
│  📊 数据导入与管理 · Knowledge Agent                     [Admin]  │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─ 数据上传区 ────────────────────────────────────────────┐     │
│  │                                                           │     │
│  │    [📁 拖拽文件到此处 或 点击上传]                           │     │
│  │                                                           │     │
│  │    支持格式: CSV, JSON, PDF, PNG, JPG, MP3/WAV, TXT       │     │
│  │    单文件最大: 50MB   |   批量上传: 支持 (最多20个文件)      │     │
│  │                                                           │     │
│  └───────────────────────────────────────────────────────────┘     │
│                                                                   │
│  ┌─ 数据处理方式选择 ──────────────────────────────────────┐     │
│  │                                                           │     │
│  │  已选文件: nov11_promo.png (2.3MB)                          │     │
│  │  检测类型: 图片 (PNG)                                       │     │
│  │                                                           │     │
│  │  处理方式 (可多选):                                        │     │
│  │  ┌─────────────────────────────────────────────────┐      │     │
│  │  │ ☑ 表格结构化解析                                 │      │     │
│  │  │    将 CSV/Excel 解析并映射到统一 Schema            │      │     │
│  │  │    Schema 模板: [客户基础信息 ▾]                   │      │     │
│  │  ├─────────────────────────────────────────────────┤      │     │
│  │  │ ☑ PDF 文本提取 (pdfplumber)                      │      │     │
│  │  │    提取 PDF 中的文字内容，保留段落结构              │      │     │
│  │  │    切片设置: chunk_size=[800], overlap=[100]      │      │     │
│  │  ├─────────────────────────────────────────────────┤      │     │
│  │  │ ☑ OCR 文字识别 (PaddleOCR)                       │      │     │
│  │  │    从图片中提取文字信息（适用于海报、截图等）        │      │     │
│  │  │    语言: [中文+英文 ▾]                             │      │     │
│  │  ├─────────────────────────────────────────────────┤      │     │
│  │  │ ☑ 图像语义理解 (Qwen-VL)                         │      │     │
│  │  │    对海报/图文混排内容进行结构化语义解析             │      │     │
│  │  │    提取: ☑活动规则 ☑目标客群 ☑时间范围             │      │     │
│  │  ├─────────────────────────────────────────────────┤      │     │
│  │  │ ☑ 向量化 Embedding (Qwen-Embedding)              │      │     │
│  │  │    将文本片段转为向量存入 Milvus 向量库             │      │     │
│  │  │    Collection: [bank_docs ▾]                      │      │     │
│  │  ├─────────────────────────────────────────────────┤      │     │
│  │  │ ☐ ASR 语音转文本 (仅音频文件)                      │      │     │
│  │  │    引擎: [FunASR ▾]  语言: [中文 ▾]               │      │     │
│  │  └─────────────────────────────────────────────────┘      │     │
│  │                                                           │     │
│  │  关联业务实体 (可选):                                       │     │
│  │  客户: [C000001 (张*明) ▾]  产品: [PROD_WHITE001 ▾]       │     │
│  │  活动: [CAMP_2026_DOUBLE11 ▾]                             │     │
│  │                                                           │     │
│  │  [▶ 开始处理]  [👁 预览结果]  [🗑 清空]                      │     │
│  └───────────────────────────────────────────────────────────┘     │
│                                                                   │
│  ┌─ 处理任务历史 ──────────────────────────────────────────┐     │
│  │  文件名              │类型  │处理方式           │状态│时间       │     │
│  │  ───────────────────┼─────┼─────────────────┼───┼───────── │     │
│  │  白金卡权益说明.pdf   │PDF   │文本提取+向量化     │✅ │07-15 10:30│    │
│  │  双十一活动海报.png   │图片  │OCR+Qwen-VL+向量化 │✅ │07-15 11:05│    │
│  │  客服录音_0715.wav   │音频  │ASR+文本提取        │⏳ │07-15 14:20│    │
│  │  交易流水_Q2.csv     │表格  │结构化解析           │✅ │07-14 09:15│    │
│  │  客户信息_批量.json   │JSON  │Schema映射          │❌ │07-14 08:00│    │
│  │                           │[查看错误详情]         │   │          │     │
│  └───────────────────────────────────────────────────────────┘     │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

**支持的数据处理策略矩阵**：

| 上传格式 | 可用处理方式 | 输出 |
|---------|------------|------|
| CSV / Excel | 表格结构化解析 → Schema 映射 | 写入 PostgreSQL + 关联 OneID |
| JSON | Schema 验证 → 结构化存储 | 按 JSON 类型入库 |
| PDF | PDF 文本提取 → Chunk → Embedding → 向量入库 | Milvus + Neo4j Document 节点 |
| PNG / JPG | OCR 文字识别 + (可选) Qwen-VL 语义理解 → Embedding | Milvus + 结构化 JSON |
| MP3 / WAV | ASR 语音转文本 → NLP 意图分析 | JSON 对话记录 + 意图标注 |
| TXT / Markdown | 直接文本解析 → Chunk → Embedding | Milvus |

### 8.4 前端技术选型建议

**推荐方案：Streamlit（快速出原型）**

```python
# 单文件多页面示例结构
import streamlit as st

st.set_page_config(page_title="Knowledge Agent Dashboard", layout="wide")

# 侧边栏导航
page = st.sidebar.radio("导航", [
    "🏠 客户360视图",
    "📊 数据导入管理",
    "📋 API文档"
])

if page == "🏠 客户360视图":
    # 搜索框
    search_query = st.text_input("搜索客户", placeholder="输入 OneID / 姓名 / 手机号...")
    if search_query:
        # 三列布局
        col1, col2, col3 = st.columns(3)
        with col1:
            st.subheader("基本信息")
            # 展示静态画像卡片
        with col2:
            st.subheader("意图雷达图")
            # Plotly 雷达图
        with col3:
            st.subheader("活跃度仪表盘")
            # 仪表盘组件
        
        # 知识图谱 (嵌入 vis-network HTML)
        st.subheader("知识图谱关系网络")
        st.components.v1.html(graph_html, height=400)
        
        # 事件时间线
        st.subheader("动态行为时间线")
        # 水平时间线组件
        
elif page == "📊 数据导入管理":
    st.header("数据导入与管理")
    uploaded_file = st.file_uploader("上传文件", type=["csv","json","pdf","png","jpg","txt"])
    if uploaded_file:
        file_type = detect_file_type(uploaded_file)
        st.info(f"检测类型: {file_type}")
        
        # 多选处理方式
        options = st.multiselect("选择处理方式", get_available_options(file_type))
        
        if st.button("开始处理"):
            with st.spinner("处理中..."):
                result = process_file(uploaded_file, options)
            st.success("处理完成!")
    
    # 处理历史表格
    st.subheader("处理任务历史")
    st.dataframe(load_processing_history())
```

**备选方案**（如需更专业的前端）：

| 组件 | 技术选择 |
|------|---------|
| 前端框架 | React 18 + TypeScript（如 C 端也用 React 可复用） |
| UI 组件库 | Ant Design / TDesign |
| 图谱可视化 | vis-network / ECharts 力导向图 / `@antv/g6` |
| 图表 | ECharts / Recharts |
| 文件上传 | react-dropzone |

---

## 九、技术栈与开源模型选型

### 9.1 整体技术栈

| 层级 | 技术 | 用途 | 备注 |
|------|------|------|------|
| **数据处理** | Python 3.11+ | 主要开发语言 | |
| | Pandas / Polars | 批处理与数据分析 | Polars 适合大数据量 |
| | Faker | Mock 数据生成 | 中文社区支持良好 |
| **API 服务** | FastAPI | REST API 框架 | 自动生成 Swagger 文档 |
| | Pydantic | 数据校验与序列化 | FastAPI 内置 |
| | Uvicorn | ASGI 服务器 | 生产级异步服务 |
| **存储层** | PostgreSQL | 结构化数据主存储 | 客户信息、交易、画像 |
| | Redis | 高速缓存 + 热数据 | 动态记忆、会话状态 |
| | Neo4j | 图数据库 | 知识图谱存储与查询 |
| | Milvus | 向量数据库 | 文档 Embedding 存储与相似检索 |
| | Elasticsearch | 全文检索 | 可选，日志搜索、关键词检索 |
| **消息队列** | Kafka / Redis Streams | 异步数据管道 | 实时事件流、C→A 反馈回流 |
| **大模型** | Qwen3-Embedding-0.6B | 文本向量化 | 本地部署，CPU 可跑 |
| | BGE-Reranker-v2-m3 | 检索结果重排序 | 提高 GraphRAG 精度 |
| | Qwen3-8B-Instruct | 意图识别 + 知识问答 | 本地 GPU 或云端 API |
| | Qwen2.5-VL-7B | 多模态图像理解（可选） | 海报/图表结构化解析 |
| **文档处理** | pdfplumber / PyPDF2 | PDF 文本提取 | |
| | python-docx | Word 文档解析 | |
| | PaddleOCR | OCR 文字识别 | 本地离线，中文效果好 |
| **前端** | Streamlit | 快速原型（大屏+管理页） | 纯 Python，一天出原型 |
| | React 18 (备选) | 正式前端 | 如需要更专业 UI |
| | vis-network | 图谱可视化 | JavaScript 库，可嵌入 Streamlit |
| | Plotly / ECharts | 图表 | 雷达图、时间线、仪表盘 |
| **编排调度** | LangChain | LLM 应用编排 | 文档加载、Chunking、Chain |
| | APScheduler | 定时任务 | 批处理调度（T+1 画像刷新） |
| **部署** | Docker + Docker Compose | 容器化部署 | Neo4j + Milvus + Redis + FastAPI 一键启动 |

### 9.2 开源模型选型对比

| 用途 | 推荐模型 | 参数量 | 部署方式 | 选型理由 |
|------|---------|:----:|---------|---------|
| **文本 Embedding** | Qwen3-Embedding-0.6B | 0.6B | 本地 CPU | 轻量、中文 C-MTEB 榜单领先、免费 |
| (备选) | BGE-M3 | 0.5B | 本地 CPU | 多语言、社区生态成熟 |
| **Reranker** | BGE-Reranker-v2-m3 | 0.5B | 本地 CPU | 大幅提高检索精度 |
| **意图识别** | Qwen3-8B-Instruct | 8B | GPU(T4/A10) 或云端 API | 中文理解和指令遵循能力强 |
| (备选) | Qwen2.5-7B-Instruct | 7B | GPU 或云端 API | 成熟稳定 |
| **知识问答** | Qwen3-8B-Instruct | 8B | GPU 或云端 API | 同上，支持长上下文 |
| **多模态理解(可选)** | Qwen2.5-VL-7B-Instruct | 7B | GPU 或云端 API | 理解海报/图表中的图文信息 |
| **ASR 语音转文本(可选)** | FunASR (Paraformer) | - | 本地 CPU | 阿里达摩院出品，中文识别率高 |

### 9.3 云端 API 方案（免 GPU 环境搭建）

如本地 GPU 资源有限，可使用云端 API 调用大模型：

| 平台 | 可用模型 | 计费方式 | 说明 |
|------|---------|---------|------|
| **阿里灵积 (DashScope)** | Qwen3-8B, Qwen-Embedding, Qwen-VL | 按 Token 计费 | 阿里官方，兼容性最好 |
| **硅基流动 (SiliconFlow)** | Qwen 系列, BGE 系列 | 按 Token 计费 | 开源模型 API 平台，性价比高 |
| **ModelScope** | 全部 Qwen 系列 | 免费（有限额） | 阿里开源社区，适合开发测试 |

```python
# DashScope API 调用示例
from dashscope import Generation, TextEmbedding

# Embedding
def get_embedding(text: str) -> list[float]:
    resp = TextEmbedding.call(
        model="text-embedding-v3",  # Qwen3-Embedding
        input=text
    )
    return resp.output["embeddings"][0]["embedding"]

# 意图识别
def classify_intent(conversation: str) -> dict:
    resp = Generation.call(
        model="qwen3-8b-instruct",
        messages=[
            {"role": "system", "content": "你是信用卡客户意图分析专家..."},
            {"role": "user", "content": conversation}
        ],
        response_format={"type": "json_object"}
    )
    return resp.output
```

---

## 十、协同集成流与标准数据接口

### 10.1 三方数据流全景

```
┌─────────────────────────────────────────────────────────────────┐
│                      端到端数据与控制流闭环                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌──────────┐          ┌──────────┐          ┌──────────┐      │
│   │  项目一   │  A → B   │  项目二   │  B → C   │  项目三   │      │
│   │ Knowledge │─────────→│ Strategy │─────────→│ Marketing│      │
│   │  Agent    │ 画像/意图 │  Agent   │ 策略包   │  Agent   │      │
│   │  (A)     │←─────────│  (B)    │←─────────│  (C)    │      │
│   └──────────┘ 反馈回流  └──────────┘ 效果归因  └──────────┘      │
│       │                                 △            │           │
│       │                                 │  C→A       │           │
│       └─────────────────────────────────┘            │           │
│                    行为反馈与画像更新                   │           │
│                                                      │           │
│                    最终用户 (APP/微信/短信)              │           │
└─────────────────────────────────────────────────────────────────┘
```

### 10.2 三条核心集成流

#### 流① A → B：认知与数据供给

```
时机：B 端 Strategy Agent 开启策略决策前
协议：REST API (同步请求-响应)

B 端调用:
  GET /api/v1/customer/{oneid}/insight    (一站式获取)

A 端返回 (CustomerInsight):
  {
    "profile": {...},       // CustomerProfile - 静态+动态画像
    "intent": {...},        // IntentVector - 当前意图向量
    "events": [...],        // EventSequence - 近期关键事件
    "knowledge": [...]      // DomainKnowledge - 相关业务规则/权益
  }

B 端消费方式：
  - profile → 了解客户基本面，判断是否在目标客群内
  - intent → 感知客户当前需求，匹配对应策略
  - events → 了解客户近期行为，确定触达时机和角度
  - knowledge → 获取合规约束和产品规则
```

#### 流② B → C：指令分发与执行

```
时机：B 端完成策略推理后
协议：REST API (同步下发)

B 端调用:
  POST /api/v3/campaign/deploy    (C 端接口，B 负责调用)

请求体 (CampaignDeploy JSON):
  {
    "campaign_metadata": {
      "campaign_id": "CAMP_2026_DOUBLE11",
      "campaign_name": "双十一分期免息大促",
      "objective": "提升分期转化率"
    },
    "audience_segment": {
      "oneids": ["UID000001", "UID000002", ...],
      "segment_size": 50000
    },
    "benefit_rule": {
      "benefit_type": "分期免息",
      "periods": [3, 6, 12],
      "free_periods": 3,
      "conditions": "指定商户单笔满3000元"
    },
    "channel_routing": [
      {"channel": "APP Push", "priority": 1, "quota_pct": 60},
      {"channel": "短信", "priority": 2, "quota_pct": 30},
      {"channel": "AI外呼", "priority": 3, "quota_pct": 10}
    ],
    "budget_allocation": {
      "total_budget": 500000,
      "max_cost_per_acquisition": 50
    },
    "content_brief": {
      "tone": "关怀+优惠引导",
      "key_points": ["前3期免息", "限时优惠", "点击查看额度"],
      "compliance_notes": ["需标注'分期有风险'", "年化利率说明"]
    }
  }
```

> **A 端在此流中的角色**：此流发生在 B 和 C 之间，A 端不直接参与。但 C 端在执行触达时如需查询客户特定知识（如该客户的权益详情），可回查 A 端的 API-05（知识检索）。

#### 流③ C → A：行为反馈与再学习

```
时机：C 端完成触达并监测到用户响应后
协议：消息队列 (异步)

C 端 → Kafka Topic: customer_feedback → A 端消费

消息格式 (FeedbackMetrics):
  {
    "oneid": "UID000001",
    "campaign_id": "CAMP_2026_DOUBLE11",
    "touchpoint": {
      "channel": "APP Push",
      "send_time": "2026-11-01T10:00:00Z",
      "content_id": "CONTENT_PUSH_001"
    },
    "engagement": {
      "impression": true,
      "impression_time": "2026-11-01T10:01:23Z",
      "click": true,
      "click_time": "2026-11-01T10:03:45Z"
    },
    "conversion": {
      "converted": true,
      "conversion_type": "分期申请",
      "conversion_amount": 12500,
      "conversion_time": "2026-11-01T12:30:00Z"
    },
    "user_response": {
      "response_type": "positive",
      "interaction_data": {
        "clicked_button": "立即查看额度",
        "page_stay_sec": 67
      }
    },
    "timestamp": "2026-11-01T12:30:00Z"
  }

A 端处理逻辑：
  1. 消费 Kafka 消息
  2. 更新客户画像（增加"分期转化"事件到记忆中）
  3. 更新图谱标签（如：客户对"分期免息"敏感度高）
  4. 刷新意图评分（分期需求已被满足 → 降低分期意图分）
  5. 可选：触发 B 端重新评估该客户
```

#### 流④ B ⇄ C：实时对话与策略动态调整

```
时机：C 端在多轮触达中监测到异常波动时
协议：REST API (同步回调)

场景示例：
  C 端发现客户在 APP 内连续 3 次关闭分期推送弹窗
  → C 端即时回调 B 端:
    POST /api/v2/strategy/adjust
    {
      "oneid": "UID000001",
      "trigger": "negative_feedback_loop",
      "detail": "客户连续3次关闭分期推送到弹窗，疑似推送疲劳",
      "current_channel": "APP Push"
    }
  → B 端重新推理:
    {
      "action": "change_strategy",
      "new_strategy": "降低推送频次，切换为权益优惠推送",
      "cooldown_period_hours": 72
    }

> A 端在此流中的角色：提供实时画像数据支持 B 端重新推理决策。
```

---

## 十一、最终交付成果清单

### 11.1 交付物总览

```
项目一：Knowledge Agent 交付清单
│
├── 📁 mock_data/                        # 模块一：Mock 数据体系
│   ├── structured/                       # 结构化数据（CSV/Parquet）
│   │   ├── customer_basic.csv            # 客户基础信息 (~10,000 行)
│   │   ├── credit_card.csv               # 信用卡信息
│   │   ├── transaction_log.csv           # 交易流水 (~500,000 行)
│   │   ├── bill_record.csv               # 账单记录
│   │   ├── crm_customer.csv              # CRM 客户关系
│   │   ├── app_events.csv                # APP 埋点事件 (~500,000 行)
│   │   ├── product_catalog.csv           # 产品目录
│   │   ├── benefit_catalog.csv           # 权益目录
│   │   ├── product_benefit_mapping.csv   # 产品-权益关联
│   │   └── campaign_catalog.csv          # 营销活动目录
│   ├── unstructured/                     # 非结构化数据
│   │   ├── asr_transcripts/              # 客服 ASR 对话 JSON (500~2000 个文件)
│   │   ├── product_docs/                 # 产品说明书 PDF (20~30 份)
│   │   └── posters/                      # 活动海报图片 + JSON 描述 (10~15 组)
│   ├── scripts/                          # 数据生成脚本
│   │   ├── generate_customers.py
│   │   ├── generate_transactions.py
│   │   ├── generate_asr.py
│   │   └── generate_products.py
│   └── README.md                         # 数据字典与 Schema 说明
│
├── 📁 oneid_engine/                      # 模块二：OneID 统一映射
│   ├── id_resolver.py                    # ID 解析与 OneID 生成核心逻辑
│   ├── id_mapping_store.py               # 映射表 CRUD 操作
│   ├── conflict_detector.py              # 冲突检测与日志
│   └── router_id.py                      # FastAPI 路由（OneID 查询接口）
│
├── 📁 profile_engine/                    # 模块三：客户画像与动态记忆
│   ├── static_builder.py                 # 静态画像构建（批处理）
│   ├── dynamic_memory.py                 # 动态记忆计算（窗口聚合）
│   ├── realtime_updater.py               # 实时事件处理（Kafka 消费）
│   ├── memory_windows.py                 # 多时间窗口管理
│   ├── scheduler.py                      # 定时任务调度
│   └── router_profile.py                 # FastAPI 路由（画像查询接口）
│
├── 📁 graphrag/                          # 模块四：GraphRAG 知识图谱
│   ├── neo4j/
│   │   ├── schema.cypher                 # 图 Schema 定义
│   │   ├── init_graph.cypher             # 初始化建图脚本
│   │   └── sample_queries.cypher         # 常用查询示例
│   ├── vector_store/
│   │   ├── doc_loader.py                 # 文档加载（PDF/Word/TXT）
│   │   ├── chunker.py                    # 文本切片
│   │   ├── embedder.py                   # 向量化（Qwen Embedding）
│   │   └── milvus_client.py              # Milvus 操作封装
│   ├── retrieval/
│   │   ├── graph_retriever.py            # 图谱检索（Cypher 查询）
│   │   ├── vector_retriever.py           # 向量相似检索
│   │   ├── fusion.py                     # 融合排序
│   │   └── reranker.py                   # Reranker 重排序
│   └── router_knowledge.py               # FastAPI 路由（知识检索接口）
│
├── 📁 intent_engine/                     # 模块五：智能意图识别
│   ├── rule_scorer.py                    # 规则评分引擎
│   ├── intent_rules.yaml                 # 意图规则配置（可热更新）
│   ├── llm_classifier.py                 # 大模型语义分类
│   ├── sentiment_analyzer.py             # 情感分析
│   ├── prompts/                          # LLM Prompt 模板
│   │   ├── intent_classify.txt
│   │   └── sentiment_analyze.txt
│   └── router_intent.py                  # FastAPI 路由（意图查询接口）
│
├── 📁 api_server/                        # 模块六：统一 API 服务
│   ├── main.py                           # FastAPI 应用入口
│   ├── config.py                         # 配置管理
│   ├── middleware.py                     # 中间件（日志、限流、CORS）
│   ├── dependencies.py                   # 依赖注入
│   ├── routers/                          # 路由模块
│   │   ├── __init__.py
│   │   ├── oneid.py
│   │   ├── customer.py
│   │   ├── knowledge.py
│   │   └── feedback.py
│   ├── schemas/                          # Pydantic 数据模型
│   │   ├── customer.py
│   │   ├── intent.py
│   │   ├── knowledge.py
│   │   └── feedback.py
│   └── requirements.txt                  # Python 依赖清单
│
├── 📁 dashboard/                         # 模块七：数据基座大屏
│   ├── app.py                            # Streamlit 主入口
│   ├── pages/                            # 多页面
│   │   ├── customer_360.py               # 客户 360 视图大屏
│   │   └── data_import.py                # 数据导入管理页
│   ├── components/                       # 可复用组件
│   │   ├── profile_card.py               # 画像卡片
│   │   ├── intent_radar.py               # 意图雷达图
│   │   ├── knowledge_graph_viz.py        # 图谱可视化（vis-network）
│   │   ├── event_timeline.py             # 事件时间线
│   │   ├── sentiment_gauge.py            # 情感仪表盘
│   │   ├── file_uploader.py              # 文件上传组件
│   │   └── processing_history.py         # 处理历史表格
│   └── utils/
│       ├── api_client.py                 # 后端 API 调用封装
│       └── graph_utils.py                # 图谱数据转换
│
├── 📁 deployment/                        # 部署相关
│   ├── docker-compose.yml                # 一键启动所有服务
│   ├── Dockerfile.api                    # API 服务镜像
│   ├── Dockerfile.dashboard              # 大屏服务镜像
│   ├── neo4j.conf                        # Neo4j 配置
│   ├── milvus.yaml                       # Milvus 配置
│   └── .env.example                      # 环境变量模板
│
└── 📁 docs/                              # 文档
    ├── architecture.md                   # 架构设计文档
    ├── api_specification.md              # API 接口规范（与 B/C 端对齐用）
    ├── data_dictionary.md                # 数据字典
    ├── deployment_guide.md               # 部署说明
    ├── integration_guide.md              # 与 B/C 端集成指南
    └── final_report.md                   # 项目总结报告（答辩用）
```

### 11.2 验收标准

| 编号 | 验收项 | 验收标准 | 演示方式 |
|:--:|------|---------|---------|
| AC-01 | Mock 数据 | 覆盖全部 10 张表 + 3 类非结构化数据，总量达标 | 展示数据目录与抽样查询 |
| AC-02 | OneID 映射 | 任意输入 ID 类型能正确返回唯一 OneID，含置信度 | API 调用演示 |
| AC-03 | 客户画像 | 搜索客户 → 展示完整静态+动态画像 JSON | 大屏搜索+展示 |
| AC-04 | 意图识别 | 展示 6 类意图评分 + 情感分析，含可解释 evidence | 大屏意图雷达图 |
| AC-05 | GraphRAG | 输入自然语言查询 → 返回图谱+向量融合的检索结果 | API 调用 + 大屏展示 |
| AC-06 | 知识图谱 | 大屏可交互式展示客户-产品-权益关系网络 | 大屏拖拽交互 |
| AC-07 | 数据导入 | 上传 PDF/CSV/图片 → 选择处理方式 → 成功入库 | 数据管理页演示 |
| AC-08 | API 文档 | Swagger UI 完整展示全部 9 个 API，可在线调试 | 打开 /docs 页面 |
| AC-09 | B 端集成 | B 端能成功调用 A 端 API 获取 CustomerInsight | 联调演示 |

---

## 十二、执行路线图与里程碑

### 12.1 六周执行计划

```
Week 1 ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░
│  Mock 数据体系设计与生成
│  ├── Day 1-2: 设计全部数据 Schema
│  ├── Day 2-4: 编写客户/交易/账单 Mock 脚本
│  ├── Day 4-5: 编写 ASR 对话/产品文档 Mock 脚本
│  └── Day 5-6: 数据质量校验与数据字典文档
│  里程碑 M1: Mock 数据集 v1.0 就绪，含完整数据字典
│
Week 2 ░░░░████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░
│  OneID 融合引擎 + 客户画像（静态）
│  ├── Day 1-3: OneID 映射表设计与实现
│  ├── Day 3-4: OneID API 接口开发与测试
│  ├── Day 4-6: 静态画像构建（批处理 Pipeline）
│  └── Day 6-7: 画像 API 接口开发
│  里程碑 M2: OneID 解析 + 静态画像查询可调用
│
Week 3 ░░░░░░░░████░░░░░░░░░░░░░░░░░░░░░░░░░░░░
│  动态记忆引擎 + 意图识别（规则层）
│  ├── Day 1-3: Redis 缓存层设计与多窗口计算
│  ├── Day 3-5: 意图规则评分引擎实现
│  ├── Day 5-6: 情感分析模块
│  └── Day 6-7: 意图 API 接口开发
│  里程碑 M3: 动态记忆 + 规则意图评分可调用
│
Week 4 ░░░░░░░░░░░░████░░░░░░░░░░░░░░░░░░░░░░░░
│  GraphRAG 知识图谱
│  ├── Day 1-2: Neo4j Schema 建图 + Cypher 查询
│  ├── Day 2-3: 文档处理流水线（PDF→Chunk→Embedding）
│  ├── Day 3-5: Milvus 向量库搭建与入库
│  ├── Day 5-6: 混合检索（图谱+向量）与融合排序
│  └── Day 6-7: 知识检索 API 接口开发
│  里程碑 M4: GraphRAG 知识检索完整链路可调用
│
Week 5 ░░░░░░░░░░░░░░░░████░░░░░░░░░░░░░░░░░░░░
│  统一 API 服务 + 大模型集成
│  ├── Day 1-2: FastAPI 统一入口搭建、中间件配置
│  ├── Day 2-4: Qwen3-8B 集成（意图语义层 + 知识问答）
│  ├── Day 4-5: C→A 反馈回流接口（Kafka 消费）
│  ├── Day 5-7: 与 B 端接口联调对齐
│  里程碑 M5: 全部 API 通过接口测试，B 端可调通
│
Week 6 ░░░░░░░░░░░░░░░░░░░░████░░░░░░░░░░░░░░░░
│  数据基座大屏 + 文档 + 集成验收
│  ├── Day 1-2: 数据导入管理页开发
│  ├── Day 2-4: 客户 360 视图大屏开发
│  ├── Day 4-5: 端到端集成测试（A→B→C→A）
│  ├── Day 5-6: 技术文档、部署文档、答辩报告
│  └── Day 6-7: 最终验收演示排练
│  里程碑 M6: 全部交付物就绪，通过集成验收
```

### 12.2 关键风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|:--:|:--:|---------|
| Neo4j/Milvus 环境搭建困难 | 中 | 高 | 提前准备 Docker Compose 一键部署脚本；降级方案：用 NetworkX + ChromaDB 替代 |
| 大模型 API 调用延迟/不稳定 | 中 | 中 | 意图识别核心链路用规则引擎兜底；LLM 仅用于增强（非主链路） |
| 与 B/C 端接口不一致 | 高 | 高 | Week 1 即输出接口契约草案，三方确认后锁定；Week 5 安排联调 |
| 前端开发时间不足 | 中 | 中 | 优先使用 Streamlit 快速出原型；React 仅作为备选 |
| Mock 数据量过大导致处理慢 | 低 | 低 | 数据量可调参；本地开发用小数据集（1000 客户），演示用全量 |

---

## 附录 A：JSON 接口契约规范

### A.1 CustomerProfile（客户画像）

```json
{
  "oneid": "UID000001",
  "static_profile": {
    "name": "string",
    "gender": "M|F",
    "age": 32,
    "city": "string",
    "occupation": "string",
    "income_level": "H|M|L",
    "education": "string",
    "lifecycle_stage": "新户|成长期|成熟期|沉睡期",
    "customer_since": "2020-03-15",
    "cards": [
      {
        "card_no": "string",
        "card_level": "普卡|金卡|白金卡|无限卡|钻石卡",
        "credit_amount": 200000.00,
        "used_amount": 156000.00,
        "usage_rate": 0.78,
        "open_date": "2020-03-20",
        "product_id": "string",
        "product_name": "string"
      }
    ],
    "risk_tags": {
      "overdue_status": "M0|M1|M2|M3+",
      "history_overdue_count": 0,
      "min_payment_frequency_3m": 0,
      "cash_advance_risk_score": 0
    },
    "value_tags": {
      "annual_consumption": 180000.00,
      "monthly_avg_consumption": 15000.00,
      "ltv_level": "高|中|低",
      "installment_contribution_12m": 1200.00
    }
  },
  "dynamic_memory": {
    "realtime_signals": [
      {
        "event_type": "search|transaction|browse|service",
        "detail": "object",
        "timestamp": "ISO8601"
      }
    ],
    "short_term_7d": {
      "total_consumption": 0,
      "transaction_count": 0,
      "active_days": 0,
      "top_search_keywords": ["string"],
      "top_browse_pages": [{"page": "string", "count": 0}]
    },
    "mid_term_30d": {
      "total_consumption": 0,
      "consumption_trend": "up|stable|down",
      "trend_change_pct": 0,
      "top_merchant_categories": [{"category": "string", "ratio": 0}],
      "top_browse_categories": [{"category": "string", "ratio": 0}]
    },
    "long_term_90d": {
      "total_consumption": 0,
      "consumption_trend": "up|stable|down",
      "activity_score": 0,
      "dormancy_risk": "low|medium|high",
      "significant_signals": ["消费降级", "境外交易增加"]
    }
  },
  "generated_at": "ISO8601"
}
```

### A.2 IntentVector（意图向量）

```json
{
  "oneid": "string",
  "updated_at": "ISO8601",
  "intents": [
    {
      "type": "分期/借贷需求|跨境/出行需求|额度/升级需求|权益/优惠需求|沉睡/流失风险|新户/激活引导",
      "score": 0,
      "confidence": "high|medium|low",
      "sub_signals": [
        {
          "signal": "string",
          "value": "string",
          "weight": 0,
          "threshold": "string"
        }
      ],
      "trend": "rising|stable|falling",
      "evidence_summary": "string"
    }
  ],
  "primary_intent": "string",
  "sentiment": {
    "overall": "positive|neutral|anxious|dissatisfied",
    "confidence": 0,
    "anxiety_score": 0,
    "satisfaction_score": 0,
    "key_evidence": "string"
  }
}
```

### A.3 EventSequence（事件流）

```json
{
  "oneid": "string",
  "window": "7d|30d|90d",
  "total_events": 0,
  "events": [
    {
      "event_id": "string",
      "event_type": "transaction|search|browse|service|milestone",
      "detail": "object",
      "timestamp": "ISO8601",
      "significance": "high|medium|low"
    }
  ]
}
```

### A.4 DomainKnowledge（业务知识）

```json
{
  "query": "string",
  "customer_id": "string|null",
  "results": [
    {
      "rank": 1,
      "source_type": "graph|vector",
      "content": "string",
      "source_doc": "string",
      "page": 0,
      "relevance_score": 0,
      "metadata": {
        "entity_type": "string",
        "entity_id": "string",
        "relation_path": "string"
      }
    }
  ],
  "graph_context": {
    "related_entities": [
      {
        "type": "Customer|Product|Benefit|Campaign|InstallmentRule",
        "id": "string",
        "name": "string"
      }
    ]
  }
}
```

---

## 附录 B：意图分类体系详细定义

### B.1 规则权重配置表

#### 意图 ①：分期/借贷需求

| 触发规则 | 信号来源 | 阈值 | 权重 | 说明 |
|---------|---------|------|:--:|------|
| 账单金额 / 授信额度比 | `bill_record` + `credit_card` | > 70% | 30 | 账单压力信号 |
| 近7天搜索"分期""手续费""最低还款" | `app_events.search_keyword` | ≥ 2 次 | 20 | 主动搜索信号 |
| 近7天客服通话涉及分期/账单 | `asr_transcripts` (LLM 判定) | ≥ 1 次 | 25 | 人工求助信号（权重高） |
| 近3月最低还款次数 | `bill_record.is_min_payment` | ≥ 1 次 | 10 | 还款能力信号 |
| 近7天浏览"分期计算器"页面次数 | `app_events.page_name` | ≥ 3 次 | 15 | 行为意图信号 |

**总分阈值**：
- ≥ 70 分 → High Confidence（高置信度分期意图）
- 40-69 分 → Medium Confidence（中等，需持续观察）
- < 40 分 → Low Confidence（暂无显著需求）

#### 意图 ②：跨境/出行需求

| 触发规则 | 信号来源 | 阈值 | 权重 |
|---------|---------|------|:--:|
| 近30天境外交易笔数 | `transaction_log.is_cross_border` | ≥ 3 笔 | 25 |
| 近7天搜索"境外""汇率""海淘""免税" | `app_events.search_keyword` | ≥ 2 次 | 20 |
| 近7天搜索旅游目的地关键词 | `app_events.search_keyword` | ≥ 3 次 | 20 |
| 近30天购买国际机票/酒店 | `transaction_log.merchant_category` = 商旅 + 境外 | ≥ 1 笔 | 20 |
| 持有境外返现权益但近3月未触发 | `credit_card` + `benefit` 交叉 | - | 15 |

#### 意图 ③：额度/升级需求

| 触发规则 | 信号来源 | 阈值 | 权重 |
|---------|---------|------|:--:|
| 近90天平均额度使用率 | `credit_card` + `transaction_log` | > 85% | 25 |
| 近7天搜索"提额""额度不够""申请提额" | `app_events.search_keyword` | ≥ 1 次 | 25 |
| 收入等级 H 但卡等 ≤ 金卡 | `customer_basic` + `credit_card` | - | 15 |
| 近30天浏览高端卡产品页 | `app_events.page_name` | ≥ 3 次 | 20 |
| 近30天有大额消费被拒付 | `transaction_log.txn_type` = 拒付 | ≥ 1 次 | 15 |

#### 意图 ④：权益/优惠需求

| 触发规则 | 信号来源 | 阈值 | 权重 |
|---------|---------|------|:--:|
| 近30天积分即将到期（≤30天） | `benefit` 系统积分规则 | - | 25 |
| 近7天浏览"权益商城"次数 | `app_events.page_name` | ≥ 5 次 | 25 |
| 近7天搜索"积分""兑换""里程""优惠" | `app_events.search_keyword` | ≥ 2 次 | 20 |
| 当前月份为生日月或下月 | `customer_basic.birthday` | 本月或下月 | 15 |
| 近30天浏览特定合作商户页面 | `app_events.page_name` | ≥ 3 次 | 15 |

#### 意图 ⑤：沉睡/流失风险

| 触发规则 | 信号来源 | 阈值 | 权重 |
|---------|---------|------|:--:|
| 连续无主动交易天数 | `transaction_log`（最近一次交易距今） | > 90 天 | 30 |
| 额度使用率持续下降（30天趋势） | `credit_card` + `transaction_log` | ↓ > 30% | 20 |
| 近30天有客服投诉记录 | `asr_transcripts`（sentiment = 不满） | ≥ 1 次 | 25 |
| 近7天搜索"销户""注销""取消" | `app_events.search_keyword` | ≥ 1 次 | 25 |

> 此意图的 score ≥ 50 时需特别注意——这是高风险信号，B 端通常需要优先处理。

#### 意图 ⑥：新户/激活引导

| 触发规则 | 信号来源 | 阈值 | 权重 |
|---------|---------|------|:--:|
| 生命周期 = 新户（开卡 0-3 月） | `crm_customer.lifecycle_stage` | - | 25 |
| 近7天活跃但未产生二次消费 | `app_events` + `transaction_log` | - | 25 |
| 卡状态 = 正常但开卡后7天无交易 | `credit_card.card_status` | 开卡 > 7天, 交易=0 | 25 |
| 绑定快捷支付但未产生交易 | `transaction_log` | 绑定后7天, 交易=0 | 15 |
| 浏览"新手引导""开卡礼"页面 | `app_events.page_name` | ≥ 1 次 | 10 |

### B.2 意图趋势判断

| 趋势 | 定义 | 对 B 端的含义 |
|------|------|-------------|
| `rising` | 近7天意图评分较前一个7天窗口上升 > 15% | 客户需求正在升温，是较好的触达时机 |
| `stable` | 变化在 ±15% 以内 | 需求稳定，可按常规策略执行 |
| `falling` | 下降 > 15% | 需求正在消退，或已被满足 |

---

> **文档结束**
>
> 本文档详细定义了项目一（企业知识引擎与记忆中心）的全部功能模块、技术方案、接口契约、交付清单与执行路线图。
> 建议在开始编码前，将本文档中的接口契约（附录 A）与 B 端、C 端负责人对齐确认，以确保三方可并行开发。
