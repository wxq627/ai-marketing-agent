# 三项目接口完整规范 — Knowledge Agent (项目一)

> **给项目二/三负责人的接口文档** | 版本 v2.0 | 2026-07-17  
> 所有接口均已在 http://localhost:8080/docs 上线, 可直接调用调试

---

## 零、三项目数据流全景（先理解整体关系）

```
┌──────────────────────────────────────────────────────────────────┐
│                      三项目闭环                                    │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐          ┌──────────────┐          ┌──────────┐│
│  │   项目一       │  A→B     │   项目二       │  B→C     │  项目三   ││
│  │  Knowledge    │────────→│  Strategy     │────────→│ Marketing ││
│  │  Agent (A)    │ 画像/意图 │  Agent (B)    │ 策略包   │  Agent (C)││
│  │               │←────────│               │←────────│          ││
│  └──────────────┘ C→A 反馈  └──────────────┘ C→B 效果  └──────────┘│
│                                                                   │
│  A 提供: 客户画像/意图/知识/渠道/频控/活动效果                      │
│  C 回传: 客户行为事件/对话摘要/转化数据/投诉                        │
└──────────────────────────────────────────────────────────────────┘
```

---

## 一、项目一 → 项目二 (A→B): 5 个接口

> B端(策略大脑)在制定营销策略时, 从A端获取客户洞察、频控状态、渠道资源、产品知识、历史效果。

### API-B1: 客户搜索 & 批量洞察

**用途**: B端按条件圈选目标客群, 一次性拿到完整客户快照。

```http
POST /api/v1/customer/search
Content-Type: application/json

{
  "age_min": 25, "age_max": 45,        ← 可选筛选条件(全部可选)
  "city": ["深圳","上海"],
  "income_level": ["M","H"],
  "card_level": ["白金卡","钻石卡"],
  "lifecycle_stage": ["成熟期"],
  "risk_level": ["low"],
  "value_level": ["high"],
  "exclude_dnc": true,                  ← 排除免打扰
  "exclude_blacklist": true,            ← 排除黑名单
  "page": 1, "page_size": 50
}
```

**返回**: 每个客户包含 5 组数据:

| 数据组 | 字段 | B端用来做什么 |
|------|------|------|
| `profile` | age/city/income/lifecycle/vip | 判断是否在目标客群内 |
| `account` | card_level/credit_amount/usage_rate | 判断推送什么等级的产品 |
| `consumption` | annual/monthly/cons_90d/30d/7d/trend/activity | 判断消费能力和趋势 |
| `risk` | overdue/churn_risk/risk_level/dormancy | 判断风险, 决定话术基调 |
| `consent` | marketing_consent/dnc/blacklist/channel_consents | **硬门槛: 决定能不能发** |
| `intent` | primary_intent + 6类评分 | **核心: 决定推什么策略** |

### API-B2: 频控检查

**用途**: B端在发送营销前, 检查客户是否超过频控上限。

```http
POST /api/v1/customer/frequency-check
Content-Type: application/json

{
  "cust_ids": ["C000001","C000002","C000007"],
  "planned_channel": "短信"
}
```

**返回**:

```json
{
  "results": [
    {
      "cust_id": "C000001",
      "can_send": true,                        ← 能不能发
      "available_channels": ["APP Push","短信","微信公众号"],
      "blocked_channels": [],                   ← 永久禁止的渠道
      "contact_count": {"1d":1,"7d":2,"30d":5}, ← 近1/7/30天触达次数
      "limits": {"global_max_per_day":2,"global_max_per_week":5}
    },
    {
      "cust_id": "C000007",
      "can_send": false,                       ← 不能发!
      "block_reason": "do_not_contact",         ← 原因
      "available_channels": []
    }
  ]
}
```

**B端使用逻辑**:
```
if can_send == false → 跳过该客户
if planned_channel not in available_channels → 换渠道
if contact_count.7d >= limits.global_max_per_week → 等下周再发
```

### API-B3: 渠道上下文

**用途**: B端做渠道选择和预算分配。

```http
GET /api/v1/channels/context
```

**返回**: 8个渠道的成本/容量/转化率:

| 渠道 | 单次成本 | 日容量 | 打开率 | 点击率 | 需授权 |
|------|:--:|:--:|:--:|:--:|:--:|
| APP Push | ¥0.02 | 500K | 18% | 6% | 是 |
| 短信 | ¥0.06 | 200K | 8% | 2% | 是 |
| 微信公众号 | ¥0.03 | 300K | 12% | 4% | 是 |
| 邮件 | ¥0.01 | 100K | 5% | 1% | 是 |
| APP内消息 | ¥0.01 | 800K | 25% | 10% | 否 |
| 电话外呼 | ¥2.50 | 5K | 35% | 15% | 是 |

**B端使用逻辑**:
```
按预算: 邮件<APP Push<短信<外呼
按转化: 外呼>APP内>APP Push>短信>邮件
按容量: APP内>APP Push>短信>邮件>外呼
```

### API-B4: 知识检索

**用途**: B端查询产品、权益、合规规则, 用于策略决策。

```http
GET /api/v1/knowledge/search?q=白金卡机场权益&top_k=10
```

**返回**: 产品文档+产品+权益+活动+合规规则的匹配结果。

### API-B5: 活动效果查询

**用途**: B端复盘历史活动ROI, 优化策略。

```http
GET /api/v1/campaign/performance?campaign_id=CAMP_2026_DOUBLE11
```

**返回**: ROI/CPA/转化数/归因收入/触达成本。

---

## 二、项目一 → 项目三 (A→C): 3 个接口

> C端(对话智能体)在跟客户聊天时, 从A端获取知识、意图、画像。

### API-C1: 知识检索

**用途**: 客户问"白金卡有什么权益", 对话智能体调用此接口获取答案。

```http
GET /api/v1/knowledge/search?q=白金卡机场权益&top_k=5
```

**C端使用流程**:
```
1. 客户发消息: "白金卡有什么权益"
2. 对话智能体调用: GET /api/v1/knowledge/search?q=白金卡权益
3. 得到: 机场贵宾厅/接送机/延误险/300精选酒店等
4. 对话智能体用LLM组织语言回复客户
```

### API-C2: 意图分类

**用途**: 客户发了一段话, 对话智能体想知道客户意图。

```http
POST /api/v1/intent/classify
Content-Type: application/json

{
  "conversation_text": "你好我账单18500太高还不上帮我查12期分期手续费"
}
```

**返回**:
```json
{
  "primary_intent": "分期/借贷需求",
  "intent_score": 72,
  "sentiment": {"overall":"焦虑","anxiety_score":75,"satisfaction_score":30},
  "key_phrases": ["分期","手续费","12期","账单","还不上"],
  "b_strategy_impact": "优先推荐低门槛分期,话术需温和关怀"
}
```

**C端使用逻辑**:
```
if sentiment == "焦虑" → 话术温和, 强调"不着急""低门槛"
if sentiment == "满意" → 话术积极, 可推升级产品
if sentiment == "不满" → 先安抚, 不推营销
```

### API-C3: 客户画像查询

**用途**: 对话智能体需要知道在和谁聊天(叫什么/持什么卡/什么等级)。

```http
GET /api/v1/customer/{oneid}/profile
```

---

## 三、项目三 → 项目一 (C→A): 2 个接口

> C端在触达客户后, 把客户的行为数据回传给A端, 更新画像和意图。

### API-CA1: 反馈事件回传

**用途**: C端每次跟客户交互后, 回传事件数据。

```http
POST /api/v1/feedback/events
Content-Type: application/json

{
  "oneid": "UID000001",
  "event_type": "click",           ← impression|click|dismiss|conversion|browse|search|unsubscribe
  "campaign_id": "CAMP_2026_DOUBLE11",
  "channel": "APP Push",
  "detail": {
    "stay_sec": 45,                 ← 停留时长(点击事件)
    "amount": 5000,                 ← 转化金额(转化事件)
    "page": "分期计算器",           ← 浏览页面(浏览事件)
    "keyword": "分期费率"           ← 搜索词(搜索事件)
  },
  "timestamp": "2026-07-17T10:30:00+08:00"
}
```

**A端处理逻辑**:

| event_type | 画像更新 | 更新窗口 |
|-----------|------|:--:|
| `impression` | 频控计数+1 | 实时<100ms |
| `click` | 活跃度+1, 点击率更新, 渠道偏好调整 | 实时<100ms |
| `dismiss` | 关闭计数+1, 连续3次→频控cooldown | 实时<100ms |
| `conversion` | 年消费+amount, 月均+amount/12, ROI更新, 意图评分重算(需求被满足→降分) | 实时: 增量 | T+1: 写入CSV |
| `browse` | 浏览偏好权重更新 | 实时+下次T+1刷新 |
| `search` | 意图信号重算, B端感知通知 | 实时<100ms |
| `unsubscribe` | channel_consent→false, 永久禁止该渠道 | 实时: 标记 | T+1: 写CSV |

### API-CA2: 批量事件回传

**用途**: C端定时把一批事件一起回传。

```http
POST /api/v1/feedback/batch
Content-Type: application/json

{
  "events": [
    {"oneid":"UID000001","event_type":"impression","campaign_id":"...","channel":"APP Push"},
    {"oneid":"UID000001","event_type":"click","campaign_id":"...","channel":"APP Push","detail":{"stay_sec":45}},
    {"oneid":"UID000002","event_type":"conversion","campaign_id":"...","channel":"短信","detail":{"amount":3000}}
  ],
  "source": "project3_marketing_agent"
}
```

---

## 四、接口速查表

### A→B (项目一给项目二)

| # | 方法 | 路径 | 用途 |
|:--:|:--:|------|------|
| B1 | POST | `/api/v1/customer/search` | 客户圈选+批量快照 |
| B2 | POST | `/api/v1/customer/frequency-check` | 频控检查 |
| B3 | GET | `/api/v1/channels/context` | 渠道成本/容量 |
| B4 | GET | `/api/v1/knowledge/search` | 产品/权益/合规知识 |
| B5 | GET | `/api/v1/campaign/performance` | 活动ROI效果 |

### A→C (项目一给项目三)

| # | 方法 | 路径 | 用途 |
|:--:|:--:|------|------|
| C1 | GET | `/api/v1/knowledge/search` | 知识问答(客户问→查知识库) |
| C2 | POST | `/api/v1/intent/classify` | 对话意图分类 |
| C3 | GET | `/api/v1/customer/{oneid}/profile` | 客户画像 |

### C→A (项目三回传给项目一)

| # | 方法 | 路径 | 用途 |
|:--:|:--:|------|------|
| CA1 | POST | `/api/v1/feedback/events` | 单条事件回传 |
| CA2 | POST | `/api/v1/feedback/batch` | 批量事件回传 |

### 补充接口 (OneID/子图/统计)

| # | 方法 | 路径 | 用途 |
|:--:|:--:|------|------|
| — | GET | `/api/v1/oneid/resolve` | 根据任意ID查OneID |
| — | GET | `/api/v1/knowledge/graph/{id}` | 子图查询 |
| — | GET | `/api/v1/knowledge/stats` | 图谱统计 |

---

## 五、字段规范（三方必须遵守）

| 规范 | 说明 |
|------|------|
| **ID用字符串** | `cust_id="C000001"`, `oneid="UID000001"`, `campaign_id="CAMP_2026_DOUBLE11"` |
| **时间ISO 8601** | `2026-07-17T10:30:00+08:00` |
| **布尔true/false** | 不是"是/否"字符串 |
| **金额单位元** | B端展示时自行转万元 |
| **data_version** | 每次API返回中包含, 答辩时说明数据版本 |
| **端口** | API:8080 | 360大屏:8501 | 图谱大屏:8502 |

---

## 六、大屏地址

| 页面 | 地址 | 用途 |
|------|------|------|
| API文档(Swagger) | http://localhost:8080/docs | 所有接口在线调试 |
| 客户360大屏 | http://localhost:8501 | 画像/动态/意图/ROI/更新 |
| 知识图谱大屏 | http://localhost:8502 | 产品权益/知识检索 |

---

> **文档版本**: v2.0 | 基于项目一 v1.0 技术方案 | 所有接口已上线可调用
