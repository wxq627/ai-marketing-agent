# 三项目接口完整规范 — Knowledge Agent (项目一)

> **给项目二/三负责人的接口文档** | 版本 v3.0 | 2026-07-17  
> API 地址: http://localhost:8080 | Swagger: http://localhost:8080/docs

---

## 零、三项目数据流全景

```
┌──────────────────────────────────────────────────────────────────┐
│                      三项目闭环                                    │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐          ┌──────────────┐          ┌──────────┐│
│  │   项目一       │  A→B     │   项目二       │  B→C     │  项目三   ││
│  │  Knowledge    │────────→│  Strategy     │────────→│ Marketing ││
│  │  Agent (A)    │ 数据供给  │  Agent (B)    │ 策略包   │  Agent (C)││
│  │               │←────────│               │←────────│          ││
│  └──────────────┘ C→A 反馈  └──────────────┘ C→B 效果  └──────────┘│
│       │                                                            │
│       │ A→C (项目三直接调用): 画像/意图/知识/情感/推荐/资格          │
│       │ C→A (项目三回传):    对话摘要/行为事件/转化/投诉             │
│       │                                                            │
└──────────────────────────────────────────────────────────────────┘
```

---

## 一、项目一 → 项目二 (A→B): 5 个接口

### API-B1: 客户搜索 & 批量洞察

```http
POST /api/v1/customer/search
```
按年龄/城市/收入/卡等级/生命周期/风险/价值圈选客群，返回完整快照(画像+消费+风险+授权+意图)。

### API-B2: 频控检查

```http
POST /api/v1/customer/frequency-check
```
批量检查客户是否可以触达、哪些渠道可用、近1/7/30天触达次数。

### API-B3: 渠道上下文

```http
GET /api/v1/channels/context
```
8个渠道的成本/容量/打开率/点击率。

### API-B4: 知识检索

```http
GET /api/v1/knowledge/search?q=白金卡权益
```
产品/权益/活动/合规规则全文检索。

### API-B5: 活动效果

```http
GET /api/v1/campaign/performance
```
历史活动ROI/CPA/转化率。

---

## 二、项目一 → 项目三 (A→C): 7 个接口

> 项目三在做对话智能体时，需要从项目一获取客户信息、意图、知识、个性化推荐。

### 场景总览

```
客户发消息 → 项目三对话智能体
              │
              ├── ① 识别客户:     GET /api/v1/oneid/resolve          "这个手机号是谁?"
              ├── ② 获取画像:     GET /api/v1/customer/{id}/profile   "他叫什么?持什么卡?"
              ├── ③ 理解意图:     GET /api/v1/customer/{id}/intent    "他想干什么?"
              ├── ④ 查知识:       GET /api/v1/knowledge/search        "这个产品有什么权益?"
              ├── ⑤ 查情感:       从 intent 返回的 sentiment 字段     "他情绪怎么样?"
              ├── ⑥ 查授权:       从 profile 返回的 consent 字段      "能给他发推送吗?"
              ├── ⑦ 综合回复:     LLM + 以上所有信息 → 个性化话术
              │
              └── 对话结束 → POST /api/v1/feedback/events 回传
```

---

### API-C1: 客户识别 (OneID解析)

**场景**: 客户通过手机号/设备ID进来，项目三需要知道他是谁。

```http
GET /api/v1/oneid/resolve?type=phone&value=138****8888
GET /api/v1/oneid/resolve?type=device_id&value=DEV_A001
```

**返回**: OneID + 所有已知ID列表 + 置信度。

---

### API-C2: 客户完整画像

**场景**: 拿到OneID后，获取客户全部信息用于个性化对话。

```http
GET /api/v1/customer/{oneid}/profile
```

**返回**: 项目三用来个性化对话的关键字段：

| 数据组 | 项目三用来做什么 | 示例 |
|------|------|------|
| `demographics` | 称呼客户、了解背景 | "张先生您好！" (姓名/性别/年龄/城市) |
| `account` | 知道客户持什么卡 | "您的白金卡享有..." (卡等级/额度/产品名) |
| `lifecycle` | 调整对话策略 | 新户→引导激活; 沉睡→唤醒话术 |
| `risk` | 判断能否营销 | 高风险→仅服务; 黑名单→不可触达 |
| `value` | 推荐匹配产品等级 | 高价值→推高端; 低价值→推入门 |
| `consent` | 判断能否发推送 | marketing_consent + 各渠道授权 |
| `consumption` | 了解消费习惯 | 年消费/月均/趋势/活跃度 |
| `search_keywords` | 了解最近关注什么 | "您最近在看分期方案?" |
| `browse_preferences` | 了解偏好页面 | "您常浏览权益商城?" |

---

### API-C3: 客户意图向量

**场景**: 客户说了一段话，项目三想知道他当前的需求倾向。

```http
GET /api/v1/customer/{oneid}/intent
```

**返回**: 

```json
{
  "primary_intent": "分期/借贷需求",
  "intents": [
    {"type": "分期/借贷需求", "score": 85, "confidence": "high"},
    {"type": "跨境/出行需求", "score": 45, "confidence": "medium"},
    {"type": "权益/优惠需求", "score": 30, "confidence": "low"}
  ],
  "sentiment": {
    "overall": "anxious",
    "anxiety_score": 65,
    "satisfaction_score": 40
  }
}
```

**项目三使用逻辑**:
```
if primary_intent == "分期/借贷需求" and score >= 70:
    → 主动提分期方案
if primary_intent == "沉睡/流失风险" and score >= 50:
    → 优先推送唤醒红包, 不要营销
if sentiment == "焦虑":
    → 话术温和, "不着急, 我们慢慢看"
```

---

### API-C4: 对话文本意图分类

**场景**: 客户刚发了一段话，实时分析这段话的意图和情感。

```http
POST /api/v1/intent/classify
Content-Type: application/json

{
  "conversation_text": "你好我账单18500太高还不上帮我查12期分期手续费"
}
```

**返回**:
```json
{
  "primary_intent": "分期/借贷需求",
  "intent_score": 72,
  "sentiment": {"overall":"焦虑","anxiety_score":75,"satisfaction_score":30},
  "key_phrases": ["分期","手续费","12期","账单","还不上"],
  "urgency": "高"
}
```

**与 API-C3 的区别**: C3 是基于客户历史数据的**长期意图**，C4 是基于当前对话文本的**即时意图**。

---

### API-C5: 知识全文检索

**场景**: 客户问产品问题，项目三需要查知识库来回答。

```http
GET /api/v1/knowledge/search?q=白金卡机场权益&top_k=5
```

**返回**: 产品文档+产品描述+权益说明+活动规则+合规条款的匹配片段。

```
客户: "白金卡有什么权益"
  → 智能体调此接口
  → 得到: 机场贵宾厅(全年60次) + 接送机(12次/年) + 延误险(2000元) + ...
  → LLM组织语言回复客户
```

---

### API-C6: 客户事件流

**场景**: 项目三想知道客户最近做了什么，用于个性化对话。

```http
GET /api/v1/customer/{oneid}/events?window=30d&limit=20
```

**返回**: 近30天交易/浏览/搜索/客服事件列表。

**项目三使用**: "您上周在星巴克消费了3次，我们有个咖啡优惠券..." / "您最近在看分期计算器，需要了解分期方案吗?"

---

### API-C7: 个性化推荐 (综合画像+意图)

**场景**: 项目三需要给客户推荐产品/权益/活动。

**方式**: 综合调用 API-C2(画像) + API-C3(意图) + API-C5(知识)，由项目三自己的LLM综合生成推荐。

**推荐逻辑参考**:
```
if 画像.value_level=="high" and 意图.分期需求>=70:
    → 推荐12期免息分期 + "尊享白金卡专属费率"
if 画像.lifecycle=="沉睡期" and 意图.流失风险>=50:
    → 推荐唤醒红包 + 关怀话术 (不要推营销)
if 画像.risk_level=="high" or 画像.blacklist==true:
    → 仅服务通知, 不推送任何营销内容
if 意图.出行需求>=40 and 画像.卡等级>="白金卡":
    → 推荐境外返现卡 + 机场贵宾厅权益
```

---

## 三、项目三 → 项目一 (C→A): 2 个接口

### API-CA1: 反馈事件回传

每次客户交互后回传。

```http
POST /api/v1/feedback/events
Content-Type: application/json

{
  "oneid": "UID000001",
  "event_type": "conversion",       ← impression|click|dismiss|conversion|browse|search|unsubscribe
  "campaign_id": "CAMP_2026_DOUBLE11",
  "channel": "APP Push",
  "detail": {
    "amount": 5000,                  ← 转化金额
    "stay_sec": 45,                  ← 停留时长
    "page": "分期计算器",            ← 浏览页面
    "keyword": "分期费率"            ← 搜索词
  },
  "timestamp": "2026-07-17T10:30:00+08:00"
}
```

**A端处理**:

| event_type | 画像实时更新 |
|-----------|------|
| impression | 频控计数+1 |
| click | 活跃度+1, 点击率更新 |
| conversion | **年消费+amount, ROI更新, 意图重算(需求满足→降分)** |
| browse | 浏览偏好权重更新 |
| search | 意图信号重算, 短期搜索词更新 |
| complaint | **churn_risk+25, 自动降频30天** |
| unsubscribe | channel_consent→false, 永久禁止 |

### API-CA2: 对话摘要回传

对话结束后回传对话摘要。

```http
POST /api/v1/feedback/conversation
Content-Type: application/json

{
  "oneid": "UID000001",
  "conversation_id": "CHAT_20260717_001",
  "summary": {
    "topic": "分期办理咨询",
    "customer_intent": "分期/借贷需求",
    "intent_confidence": 0.92,
    "sentiment": "neutral_to_satisfied",
    "resolution": "成功办理12期分期",
    "key_phrases": ["12期","手续费0.45%","每月1652元"],
    "customer_concerns": ["手续费是否划算","能否提前还款"]
  },
  "updated_intent_signals": [
    {"intent_type": "分期/借贷需求", "score_delta": -0.30, "reason": "需求已被满足"},
    {"intent_type": "额度/升级需求", "score_delta": 0.15, "reason": "分期后额度使用率上升"}
  ]
}
```

---

## 四、接口速查表

### A→B (项目一给项目二)

| # | 方法 | 路径 | 用途 |
|:--:|:--:|------|------|
| B1 | POST | `/api/v1/customer/search` | 客户圈选+批量快照 |
| B2 | POST | `/api/v1/customer/frequency-check` | 频控检查 |
| B3 | GET | `/api/v1/channels/context` | 渠道成本/容量 |
| B4 | GET | `/api/v1/knowledge/search` | 产品/权益知识 |
| B5 | GET | `/api/v1/campaign/performance` | 活动ROI效果 |

### A→C (项目一给项目三)

| # | 方法 | 路径 | 用途 |
|:--:|:--:|------|------|
| C1 | GET | `/api/v1/oneid/resolve` | **客户识别**(手机号→OneID) |
| C2 | GET | `/api/v1/customer/{oneid}/profile` | **完整画像**(姓名/卡/消费/偏好/授权) |
| C3 | GET | `/api/v1/customer/{oneid}/intent` | **意图向量**(6类评分+情感) |
| C4 | POST | `/api/v1/intent/classify` | **即时意图**(对话文本→意图+情感) |
| C5 | GET | `/api/v1/knowledge/search` | **知识检索**(产品/权益/条款问答) |
| C6 | GET | `/api/v1/customer/{oneid}/events` | **事件流**(近30天行为) |
| C7 | — | (综合C2+C3+C5) | **个性化推荐**(项目三LLM综合生成) |

### C→A (项目三回传给项目一)

| # | 方法 | 路径 | 用途 |
|:--:|:--:|------|------|
| CA1 | POST | `/api/v1/feedback/events` | 行为事件回传(点击/转化/投诉) |
| CA2 | POST | `/api/v1/feedback/conversation` | 对话摘要回传+意图更新信号 |

---

## 五、项目三工作流示例

```python
import requests
BASE = "http://localhost:8080"

class ConversationalAgent:
    def handle_message(self, phone, message):
        # 步骤1: 识别客户
        r = requests.get(f"{BASE}/api/v1/oneid/resolve", params={"type":"phone","value":phone})
        oneid = r.json()["oneid"]

        # 步骤2: 获取画像
        profile = requests.get(f"{BASE}/api/v1/customer/{oneid}/profile").json()
        name = profile["static_profile"]["demographics"]["name"]
        card = profile["static_profile"]["account"]["primary_card_level"]

        # 步骤3: 获取意图
        intent = requests.get(f"{BASE}/api/v1/customer/{oneid}/intent").json()
        primary = intent["primary_intent"]
        sentiment = intent["sentiment"]["overall"]

        # 步骤4: 分析当前消息意图
        classify = requests.post(f"{BASE}/api/v1/intent/classify",
            json={"conversation_text": message}).json()

        # 步骤5: 查知识(如果问到产品)
        if any(kw in message for kw in ["权益","产品","白金","分期"]):
            knowledge = requests.get(f"{BASE}/api/v1/knowledge/search",
                params={"q": message, "top_k": 5}).json()

        # 步骤6: 检查授权
        consent = profile["static_profile"]["consent"]
        can_market = consent["marketing_consent"] and not consent["dnc_list"]

        # 步骤7: 综合生成回复
        tone = "温和关怀" if sentiment=="焦虑" else ("积极" if sentiment=="满意" else "标准")
        reply = f"{name}您好！"  # + LLM基于以上所有信息生成的个性化回复

        # 步骤8: 回传对话摘要
        requests.post(f"{BASE}/api/v1/feedback/conversation", json={...})

        return reply
```

---

## 六、字段规范

| 规范 | 说明 |
|------|------|
| ID用字符串 | `cust_id="C000001"`, `oneid="UID000001"` |
| 时间ISO 8601 | `2026-07-17T10:30:00+08:00` |
| 布尔true/false | 不是"是/否" |
| 金额单位元 | B/C端展示时自行转换 |

---

## 七、服务地址

| 服务 | 地址 |
|------|------|
| API + Swagger | http://localhost:8080/docs |
| 客户360大屏 | http://localhost:8501 |
| 知识图谱大屏 | http://localhost:8502 |

---

> **版本 v3.0** | 项目二: 5接口 | 项目三: 7接口 + 2回传 | 共14个接口
